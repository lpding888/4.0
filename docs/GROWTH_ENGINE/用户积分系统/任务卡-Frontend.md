# 任务卡 - 前端开发 (用户积分系统)

> **系统**: 用户积分系统
> **负责人**: Frontend Dev Skill
> **预计工期**: 4天
> **优先级**: P0
> **依赖**: 后端API接口开发完成

---

## 📋 任务概述

开发用户端积分系统的4个页面，包括积分中心首页、兑换配额页、积分商城页、积分明细页。

**技术栈**：
- Next.js 14 (App Router)
- React 18
- TailwindCSS
- TypeScript
- SWR (数据获取和缓存)

**设计风格**：
- 暗色主题 (bg-gradient-to-br from-gray-900 to-black)
- 玻璃卡片效果 (backdrop-blur-lg)
- 霓虹青色边框 (border-cyan-400)
- 渐变按钮 (bg-gradient-to-r from-cyan-500 to-blue-500)

---

## 🎨 页面设计

### 页面1: 积分中心首页

**路由**: `/points`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ [返回]  积分中心                         │
├─────────────────────────────────────────┤
│ 【我的积分卡片】                         │
│  - 可用积分: 850                        │
│  - 累计获得: 1,500                      │
│  - 已使用: 500 | 已过期: 150            │
│  - 3个操作按钮: 兑换配额/积分商城/积分明细│
├─────────────────────────────────────────┤
│ 【每日签到卡片】                         │
│  - 连续签到天数和今日可获得积分          │
│  - 签到按钮                             │
│  - 本周签到日历                         │
├─────────────────────────────────────────┤
│ 【任务中心卡片】                         │
│  - 6个任务的完成状态和积分奖励           │
│  - 任务类型: 完成/进行中/未开始          │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询积分账户（GET /api/points/account）
2. 每日签到（POST /api/points/checkin）
3. 查询任务列表（前端配置，对接后端校验）

**UI组件**：
- PointsAccountCard: 积分账户卡片
- CheckinCard: 签到卡片（包含签到日历）
- TaskCenterCard: 任务中心卡片（任务列表）

**交互逻辑**：
- 签到按钮点击后调用签到API，成功后刷新账户数据和签到状态
- 任务按钮点击后调用任务完成API（部分任务需要跳转到其他页面）
- 使用SWR自动刷新数据（5秒轮询）

---

### 页面2: 兑换配额页

**路由**: `/points/redeem-quota`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ [返回]  兑换配额                         │
├─────────────────────────────────────────┤
│ 当前可用积分: 850                        │
│ 兑换比例: 100积分 = 1个处理配额          │
├─────────────────────────────────────────┤
│ 【兑换表单卡片】                         │
│  - 兑换数量输入框 (1-50)                │
│  - 需要积分显示                         │
│  - 剩余积分显示                         │
│  - 确认兑换按钮                         │
├─────────────────────────────────────────┤
│ 【兑换说明】                            │
│  - 最低兑换/最高兑换/到账时间           │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 输入兑换数量，实时计算所需积分和剩余积分
2. 校验兑换数量（1-50之间，积分是否足够）
3. 提交兑换（POST /api/points/redeem/quota）
4. 兑换成功后弹窗提示并跳转回积分中心

**表单校验**：
- 数量必须是整数
- 数量范围：1-50
- 积分不足时禁用按钮并提示

---

### 页面3: 积分商城页

**路由**: `/points/mall`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ [返回]  积分商城                         │
│ 可用积分: 850                           │
├─────────────────────────────────────────┤
│ 【商品卡片网格 (2列)】                  │
│  ┌──────────┐  ┌──────────┐           │
│  │ 50元优惠券│  │100元优惠券│           │
│  │ 500积分   │  │ 900积分   │           │
│  │[立即兑换] │  │[立即兑换] │           │
│  └──────────┘  └──────────┘           │
│  ┌──────────┐  ┌──────────┐           │
│  │ 1个月会员 │  │ 3个月会员 │           │
│  └──────────┘  └──────────┘           │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询商品列表（GET /api/points/mall/items）
2. 点击兑换按钮弹出确认弹窗
3. 确认后提交兑换（POST /api/points/mall/redeem）
4. 兑换成功后显示兑换结果（优惠券码/会员延长天数）

**UI组件**：
- MallItemCard: 商品卡片
- RedeemModal: 兑换确认弹窗
- RedeemSuccessModal: 兑换成功弹窗

**特殊处理**：
- 优惠券兑换成功后显示优惠券码和有效期
- 会员兑换成功后显示延长天数
- 积分不足时禁用兑换按钮

---

### 页面4: 积分明细页

**路由**: `/points/records`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ [返回]  积分明细                         │
├─────────────────────────────────────────┤
│ 【筛选标签】                            │
│ [全部] [获得] [消费] [过期]             │
├─────────────────────────────────────────┤
│ 【积分记录列表】                         │
│  ┌────────────────────────────────────┐│
│  │ 连续签到第3天              +6积分   ││
│  │ 2025-10-30 08:00         有效期365天││
│  └────────────────────────────────────┘│
│  ┌────────────────────────────────────┐│
│  │ 兑换8个处理配额            -800积分  ││
│  │ 2025-10-30 10:00                   ││
│  └────────────────────────────────────┘│
├─────────────────────────────────────────┤
│ 【分页器】                              │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询积分明细（GET /api/points/records?change_type=&page=1）
2. 点击筛选标签切换类型（earn/consume/expire）
3. 分页加载更多数据
4. 下拉刷新

**UI组件**：
- RecordFilterTabs: 筛选标签
- RecordItem: 积分记录卡片
- Pagination: 分页器

**数据展示**：
- 获得积分显示绿色+号
- 消费积分显示红色-号
- 过期积分显示灰色
- 获得积分显示有效期

---

## 📂 文件结构

```
frontend/src/app/
├── points/
│   ├── page.tsx                        // 积分中心首页
│   ├── redeem-quota/
│   │   └── page.tsx                    // 兑换配额页
│   ├── mall/
│   │   └── page.tsx                    // 积分商城页
│   └── records/
│       └── page.tsx                    // 积分明细页
├── components/
│   └── points/
│       ├── PointsAccountCard.tsx       // 积分账户卡片
│       ├── CheckinCard.tsx             // 签到卡片
│       ├── TaskCenterCard.tsx          // 任务中心卡片
│       ├── MallItemCard.tsx            // 商品卡片
│       ├── RedeemModal.tsx             // 兑换确认弹窗
│       ├── RedeemSuccessModal.tsx      // 兑换成功弹窗
│       ├── RecordFilterTabs.tsx        // 积分明细筛选标签
│       ├── RecordItem.tsx              // 积分记录卡片
│       └── Pagination.tsx              // 分页器
└── lib/
    └── api/
        └── points.ts                   // 积分API封装
```

---

## 🔧 API封装

**文件**: `frontend/src/lib/api/points.ts`

```typescript
// 查询积分账户
export async function getPointsAccount() {
  const res = await fetch('/api/points/account', {
    headers: { Authorization: `Bearer ${getToken()}` }
  });
  return res.json();
}

// 每日签到
export async function checkin() {
  const res = await fetch('/api/points/checkin', {
    method: 'POST',
    headers: { Authorization: `Bearer ${getToken()}` }
  });
  return res.json();
}

// 兑换配额
export async function redeemQuota(quotaCount: number) {
  const res = await fetch('/api/points/redeem/quota', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${getToken()}`
    },
    body: JSON.stringify({ quota_count: quotaCount })
  });
  return res.json();
}

// 查询商城商品
export async function getMallItems(itemType?: string) {
  const url = itemType
    ? `/api/points/mall/items?item_type=${itemType}`
    : '/api/points/mall/items';
  const res = await fetch(url, {
    headers: { Authorization: `Bearer ${getToken()}` }
  });
  return res.json();
}

// 兑换商城商品
export async function redeemMallItem(itemId: string) {
  const res = await fetch('/api/points/mall/redeem', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${getToken()}`
    },
    body: JSON.stringify({ item_id: itemId })
  });
  return res.json();
}

// 查询积分明细
export async function getPointsRecords(changeType?: string, page = 1, pageSize = 20) {
  const params = new URLSearchParams({
    page: String(page),
    page_size: String(pageSize)
  });
  if (changeType) params.append('change_type', changeType);

  const res = await fetch(`/api/points/records?${params}`, {
    headers: { Authorization: `Bearer ${getToken()}` }
  });
  return res.json();
}
```

---

## 🎯 开发要点

### 1. 签到日历组件

显示本周7天的签到状态（✅已签到，⭕未签到）

**实现思路**：
- 查询用户最近7天的签到记录
- 按日期匹配显示对应图标
- 今天如果已签到显示✅，未签到显示"签到"按钮

---

### 2. 任务列表动态状态

任务分为3种状态：
- ✅ 已完成（绿色对勾）
- 🔄 进行中（显示进度，如"今日0/3"）
- ⭕ 未开始（灰色圆圈）

**数据来源**：
前端配置任务列表，调用后端接口校验完成状态

---

### 3. 实时计算积分

兑换配额页输入数量时，实时计算：
- 需要积分 = 数量 × 100
- 剩余积分 = 可用积分 - 需要积分
- 如果剩余积分<0，显示红色并禁用按钮

---

### 4. 错误处理

所有API调用都需要处理错误：
- 401: Token过期，跳转登录页
- 40001: 今天已签到，显示提示
- 40002: 积分不足，显示提示
- 500: 服务器错误，显示通用错误提示

---

### 5. 加载状态

所有数据加载时显示Loading骨架屏，提升用户体验

---

## ✅ 验收标准

- [ ] 4个页面全部开发完成
- [ ] 所有API对接成功
- [ ] 签到功能正常（每天只能签到一次）
- [ ] 兑换配额功能正常（积分扣除+配额增加）
- [ ] 商城兑换功能正常（优惠券/会员）
- [ ] 积分明细筛选和分页正常
- [ ] 响应式设计适配手机端
- [ ] 错误处理完善
- [ ] Loading状态完善

---

## 📚 参考资料

- PRD文档: [PRD-用户积分系统.md](./PRD-用户积分系统.md)
- 后端API文档: [任务卡-Backend.md](./任务卡-Backend.md)
- TailwindCSS文档: https://tailwindcss.com/docs
- SWR文档: https://swr.vercel.app/

---

**任务完成后，通知QA团队开始测试！**
