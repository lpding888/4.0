# 任务卡 - 管理员前端开发 (用户积分系统)

> **系统**: 用户积分系统
> **负责人**: Admin Frontend Dev Skill
> **预计工期**: 3天
> **优先级**: P1
> **依赖**: 后端管理员API接口开发完成

---

## 📋 任务概述

开发管理员端积分系统的4个页面，包括积分统计大盘、用户积分管理、积分商城管理、积分设置页。

**技术栈**：
- Next.js 14 (App Router)
- React 18
- TailwindCSS
- TypeScript
- Recharts (图表库)

---

## 🎨 页面设计

### 页面1: 积分统计大盘

**路由**: `/admin/points/dashboard`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ 积分系统数据大盘                         │
├─────────────────────────────────────────┤
│ 【核心指标卡片】                         │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│ │总发放│ │总消费│ │过期  │ │签到人次│   │
│ │1.25M│ │850K │ │50K  │ │8.5K   │   │
│ └─────┘ └─────┘ └─────┘ └─────┘       │
├─────────────────────────────────────────┤
│ 【积分发放趋势图】(最近30天折线图)        │
├─────────────────────────────────────────┤
│ 【积分消费分布饼图】                     │
│  - 兑换配额: 65%                        │
│  - 兑换优惠券: 20%                      │
│  - 兑换会员: 15%                        │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询统计数据（GET /api/admin/points/statistics）
2. 展示核心指标卡片
3. 绘制趋势图和分布图

**UI组件**：
- StatCard: 统计指标卡片
- TrendChart: 趋势折线图（Recharts）
- DistributionChart: 分布饼图（Recharts）

---

### 页面2: 用户积分管理

**路由**: `/admin/points/users`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ 用户积分管理                             │
├─────────────────────────────────────────┤
│ 【搜索栏】                              │
│ [用户ID/手机号] [搜索]                   │
├─────────────────────────────────────────┤
│ 【用户列表表格】                         │
│ ┌──────────────────────────────────────┐│
│ │用户ID│手机号│可用积分│累计│已使用│操作││
│ ├──────────────────────────────────────┤│
│ │user_1│138**│  850  │1500│ 500 │详情││
│ │user_2│139**│ 1200  │2000│ 800 │详情││
│ └──────────────────────────────────────┘│
├─────────────────────────────────────────┤
│ 【操作按钮】                            │
│ [手动调整积分] [冻结积分] [解冻积分]     │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询用户列表（GET /api/admin/points/users?search=&page=1）
2. 搜索用户（按user_id或phone）
3. 手动调整积分（POST /api/admin/points/adjust）
4. 冻结/解冻积分（POST /api/admin/points/freeze|unfreeze）

**弹窗表单**：
- AdjustPointsModal: 手动调整积分弹窗
- FreezePointsModal: 冻结积分弹窗
- UnfreezePointsModal: 解冻积分弹窗

---

### 页面3: 积分商城管理

**路由**: `/admin/points/mall`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ 积分商城管理                             │
├─────────────────────────────────────────┤
│ [新增商品]                              │
├─────────────────────────────────────────┤
│ 【商品列表表格】                         │
│ ┌──────────────────────────────────────┐│
│ │商品名称│所需积分│库存│状态│操作      ││
│ ├──────────────────────────────────────┤│
│ │50元优惠券│500│不限│上架│编辑/下架  ││
│ │1个月会员│2000│不限│上架│编辑/下架  ││
│ └──────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询商品列表（GET /api/admin/points/mall/items）
2. 新增商品（POST /api/admin/points/mall/items）
3. 编辑商品（PUT /api/admin/points/mall/items/:id）
4. 上架/下架（PUT /api/admin/points/mall/items/:id/status）

**弹窗表单**：
- AddItemModal: 新增商品弹窗
- EditItemModal: 编辑商品弹窗

---

### 页面4: 积分设置页

**路由**: `/admin/points/settings`

**页面结构**：
```
┌─────────────────────────────────────────┐
│ 积分系统设置                             │
├─────────────────────────────────────────┤
│ 【基础设置】                            │
│ • 积分有效期: [365] 天                  │
│ • 购买会员积分比例: 1:[10]              │
│ • 积分兑换配额比例: [100]积分=1配额     │
├─────────────────────────────────────────┤
│ 【签到设置】                            │
│ • 首次签到: [2] 积分                    │
│ • 连续签到递增: ✅ 启用                 │
│ • 第5天及以后: [10] 积分                │
├─────────────────────────────────────────┤
│ 【任务设置】                            │
│ • 注册奖励: [100] 积分                  │
│ • 完善资料: [20] 积分                   │
│ • 首次使用: [30] 积分                   │
│ • 分享作品: [10] 积分 (每天限[3]次)     │
│ • 邀请好友: [50] 积分 (每月限[5]人)     │
├─────────────────────────────────────────┤
│ [保存设置]                              │
└─────────────────────────────────────────┘
```

**核心功能**：
1. 查询当前设置（GET /api/admin/points/settings）
2. 更新设置（PUT /api/admin/points/settings）

**表单校验**：
- 所有积分值必须>0
- 天数必须>0
- 比例必须>0

---

## 📂 文件结构

```
frontend/src/app/
├── admin/
│   └── points/
│       ├── dashboard/
│       │   └── page.tsx                // 积分统计大盘
│       ├── users/
│       │   └── page.tsx                // 用户积分管理
│       ├── mall/
│       │   └── page.tsx                // 积分商城管理
│       └── settings/
│           └── page.tsx                // 积分设置
└── components/
    └── admin/
        └── points/
            ├── StatCard.tsx            // 统计指标卡片
            ├── TrendChart.tsx          // 趋势图
            ├── DistributionChart.tsx   // 分布图
            ├── AdjustPointsModal.tsx   // 调整积分弹窗
            ├── FreezePointsModal.tsx   // 冻结积分弹窗
            ├── AddItemModal.tsx        // 新增商品弹窗
            └── EditItemModal.tsx       // 编辑商品弹窗
```

---

## 🔧 API封装

**文件**: `frontend/src/lib/api/admin-points.ts`

```typescript
// 查询统计数据
export async function getStatistics() {
  const res = await fetch('/api/admin/points/statistics', {
    headers: { Authorization: `Bearer ${getAdminToken()}` }
  });
  return res.json();
}

// 查询用户积分列表
export async function getUserPoints(search?: string, page = 1) {
  const params = new URLSearchParams({ page: String(page) });
  if (search) params.append('search', search);

  const res = await fetch(`/api/admin/points/users?${params}`, {
    headers: { Authorization: `Bearer ${getAdminToken()}` }
  });
  return res.json();
}

// 手动调整积分
export async function adjustPoints(userId: string, changeAmount: number, reason: string) {
  const res = await fetch('/api/admin/points/adjust', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${getAdminToken()}`
    },
    body: JSON.stringify({ user_id: userId, change_amount: changeAmount, reason })
  });
  return res.json();
}

// 冻结积分
export async function freezePoints(userId: string, amount: number, reason: string) {
  const res = await fetch('/api/admin/points/freeze', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${getAdminToken()}`
    },
    body: JSON.stringify({ user_id: userId, amount, reason })
  });
  return res.json();
}
```

---

## 🎯 开发要点

### 1. 图表库使用

使用Recharts绘制趋势图和饼图：

```tsx
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';

<LineChart data={trendData}>
  <CartesianGrid strokeDasharray="3 3" />
  <XAxis dataKey="date" />
  <YAxis />
  <Tooltip />
  <Legend />
  <Line type="monotone" dataKey="granted" stroke="#06b6d4" name="发放积分" />
  <Line type="monotone" dataKey="consumed" stroke="#ef4444" name="消费积分" />
</LineChart>
```

---

### 2. 表格分页

用户列表和商品列表都需要支持分页，每页显示20条数据

---

### 3. 权限校验

所有管理员端页面都需要校验管理员权限，非管理员跳转到登录页

---

## ✅ 验收标准

- [ ] 4个页面全部开发完成
- [ ] 统计大盘数据展示正常
- [ ] 趋势图和饼图绘制正常
- [ ] 用户积分管理功能正常（搜索/调整/冻结/解冻）
- [ ] 商城管理功能正常（新增/编辑/上下架）
- [ ] 积分设置功能正常（保存配置）
- [ ] 权限校验正常
- [ ] 表单校验完善

---

**任务完成后，通知QA团队开始测试管理员端功能！**
