# 本地环境功能过滤方案

## 🎯 问题描述

**现状**：
- ✅ 后端已有Mock机制（`imageProcessService` 检测到本地环境会自动使用Mock）
- ❌ 前端没有环境判断，本地测试时仍然显示需要腾讯云数据万象的功能
- ❌ 用户点击功能后，虽然后端用Mock返回假数据，但体验不好

**问题**：
数据万象的很多功能（商品抠图、白底处理、智能增强等）需要真实的腾讯云环境才能正常工作，但在本地测试环境：
- 后端配置是假密钥（`minioadmin`）
- 后端会自动使用Mock模式返回假数据
- 前端还是显示这些功能卡片，用户点击后会看到假数据，体验很差

**期望**：
本地测试环境不显示需要真实第三方服务的功能，或者明确标记为"Mock模式"。

---

## 💡 解决方案（3种）

### 方案1：前端根据环境变量过滤功能（推荐）

#### 实现思路
1. 在 `feature_definitions` 表添加字段 `requires_production`（是否需要生产环境）
2. 本地环境的功能接口返回时，自动过滤掉 `requires_production=true` 的功能
3. 或者前端根据 `NODE_ENV` 判断是否显示

#### 数据库迁移
```sql
-- 添加字段
ALTER TABLE feature_definitions
ADD COLUMN requires_production BOOLEAN DEFAULT FALSE
COMMENT '是否需要生产环境（本地Mock无法正常使用）';

-- 标记需要腾讯云数据万象的功能
UPDATE feature_definitions
SET requires_production = TRUE
WHERE feature_id IN ('basic_clean', 'other_data_image_features');
```

#### 后端实现
```javascript
// backend/src/controllers/feature.controller.js
async getAll(req, res, next) {
  try {
    const { enabled } = req.query;

    let features = await Feature.findAll({
      where: {
        is_enabled: enabled === 'true',
        deleted_at: null
      }
    });

    // 本地环境过滤需要生产环境的功能
    if (process.env.NODE_ENV === 'development' && process.env.COS_REGION === 'local') {
      features = features.filter(f => !f.requires_production);
      logger.info('[FeatureController] 本地环境，已过滤需要生产环境的功能');
    }

    res.json({
      success: true,
      features,
      environment: process.env.NODE_ENV
    });
  } catch (error) {
    next(error);
  }
}
```

#### 优点
- ✅ 简单直接
- ✅ 不需要前端判断
- ✅ 本地测试环境只显示可用功能

#### 缺点
- ⚠️ 需要数据库迁移
- ⚠️ 需要给每个功能标记

---

### 方案2：功能卡片显示Mock标识

#### 实现思路
1. 后端在返回功能列表时，附带 `environment` 信息
2. 前端根据环境信息，给需要生产环境的功能添加"Mock模式"标识
3. 用户点击时提示"当前为测试环境，将返回模拟数据"

#### 前端实现
```typescript
// frontend/src/components/FeatureCard.tsx
import { Badge } from 'antd';

export default function FeatureCard({ feature, environment }) {
  const isMockMode = environment === 'development' && feature.requires_production;

  return (
    <Badge.Ribbon
      text={isMockMode ? "Mock模式" : null}
      color="orange"
    >
      <Card>
        <h3>{feature.title}</h3>
        <p>{feature.description}</p>
        {isMockMode && (
          <Alert
            type="warning"
            message="测试环境，将返回模拟数据"
            showIcon
          />
        )}
      </Card>
    </Badge.Ribbon>
  );
}
```

#### 优点
- ✅ 不隐藏功能，开发者可以测试流程
- ✅ 明确标识Mock状态
- ✅ 方便测试UI和交互

#### 缺点
- ⚠️ 用户体验不如方案1（会看到Mock标识）

---

### 方案3：通过环境变量完全控制（最简单）

#### 实现思路
1. 前端读取环境变量 `NEXT_PUBLIC_MOCK_MODE`
2. 如果是Mock模式，前端自己过滤功能卡片

#### 前端环境变量
```bash
# frontend/.env.local
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_MOCK_MODE=true  # 本地环境标识
```

#### 前端实现
```typescript
// frontend/src/app/workspace/page.tsx
const isMockMode = process.env.NEXT_PUBLIC_MOCK_MODE === 'true';

// 过滤需要生产环境的功能
const filteredFeatures = features.filter(feature => {
  if (isMockMode && feature.requires_production) {
    return false; // 本地环境不显示
  }
  return true;
});
```

#### 优点
- ✅ 最简单，不需要改后端
- ✅ 不需要数据库迁移
- ✅ 前端完全控制

#### 缺点
- ⚠️ 需要手动维护哪些功能需要生产环境

---

## 🎯 推荐方案：方案1 + 方案3 组合

### 为什么组合？
1. **方案1（数据库标记）**：清晰定义哪些功能需要生产环境
2. **方案3（前端环境变量）**：快速实现，不需要等数据库迁移

### 实施步骤

#### 第一步：快速实现（使用方案3）

**1. 前端添加环境变量**
```bash
# frontend/.env.local
NEXT_PUBLIC_MOCK_MODE=true
```

**2. 前端硬编码需要过滤的功能**
```typescript
// frontend/src/utils/featureFilter.ts
const PRODUCTION_ONLY_FEATURES = [
  'basic_clean',          // 服装清理增强（需要数据万象）
  'advanced_enhance',     // 高级增强（需要数据万象）
  // 添加其他需要真实第三方服务的功能
];

export function filterFeaturesByEnvironment(features: Feature[]) {
  const isMockMode = process.env.NEXT_PUBLIC_MOCK_MODE === 'true';

  if (!isMockMode) {
    return features; // 生产环境显示所有
  }

  // 本地环境过滤
  return features.filter(f => !PRODUCTION_ONLY_FEATURES.includes(f.feature_id));
}
```

**3. 在页面中使用**
```typescript
// frontend/src/app/workspace/page.tsx
import { filterFeaturesByEnvironment } from '@/utils/featureFilter';

// 获取功能后过滤
const fetchFeatures = async () => {
  const response = await api.features.getAll({ enabled: true });
  const filteredFeatures = filterFeaturesByEnvironment(response.features);
  setFeatures(filteredFeatures);
};
```

#### 第二步：长期优化（使用方案1）

**1. 数据库迁移**
```bash
cd backend
npm run db:migrate:make add_requires_production_to_features
```

```javascript
// migration文件
exports.up = function(knex) {
  return knex.schema.table('feature_definitions', (table) => {
    table.boolean('requires_production')
      .defaultTo(false)
      .comment('是否需要生产环境（本地Mock无法正常使用）');
  });
};

exports.down = function(knex) {
  return knex.schema.table('feature_definitions', (table) => {
    table.dropColumn('requires_production');
  });
};
```

**2. 更新种子数据**
```javascript
// backend/src/db/seeds/003_feature_definitions.js
{
  feature_id: 'basic_clean',
  title: '服装清理增强',
  requires_production: true,  // 标记需要生产环境
  // ...
}
```

**3. 后端接口过滤**
```javascript
// backend/src/controllers/feature.controller.js
async getAll(req, res, next) {
  try {
    let features = await Feature.findAll({
      where: { is_enabled: true, deleted_at: null }
    });

    // 本地环境自动过滤
    const isLocal = process.env.NODE_ENV === 'development'
      && process.env.COS_REGION === 'local';

    if (isLocal) {
      features = features.filter(f => !f.requires_production);
      logger.info('[Features] 本地环境，已过滤需要生产环境的功能');
    }

    res.json({
      success: true,
      features,
      environment: {
        mode: process.env.NODE_ENV,
        isLocal
      }
    });
  } catch (error) {
    next(error);
  }
}
```

---

## 📊 三种方案对比

| 方案 | 实施难度 | 效果 | 维护成本 | 推荐度 |
|------|---------|------|---------|--------|
| **方案1：数据库标记** | 中 | 最好 | 低 | ⭐⭐⭐⭐⭐ |
| **方案2：Mock标识** | 低 | 中 | 中 | ⭐⭐⭐ |
| **方案3：环境变量** | 低 | 好 | 中 | ⭐⭐⭐⭐ |
| **方案1+3组合** | 中 | 最好 | 低 | ⭐⭐⭐⭐⭐ |

---

## 🔧 快速实施（立即可用）

### Step 1：前端添加环境变量
```bash
cd frontend

# 修改 .env.local
cat >> .env.local << 'EOF'

# Mock模式标识
NEXT_PUBLIC_MOCK_MODE=true
EOF
```

### Step 2：创建功能过滤工具
```bash
mkdir -p frontend/src/utils
cat > frontend/src/utils/featureFilter.ts << 'EOF'
import { Feature } from '@/types';

// 需要生产环境的功能列表
const PRODUCTION_ONLY_FEATURES = [
  'basic_clean',  // 服装清理增强（腾讯数据万象）
];

/**
 * 根据环境过滤功能
 */
export function filterFeaturesByEnvironment(features: Feature[]): Feature[] {
  const isMockMode = process.env.NEXT_PUBLIC_MOCK_MODE === 'true';

  if (!isMockMode) {
    // 生产环境，显示所有功能
    return features;
  }

  // 本地环境，过滤需要生产环境的功能
  console.log('[FeatureFilter] Mock模式，过滤需要生产环境的功能');
  return features.filter(f => !PRODUCTION_ONLY_FEATURES.includes(f.feature_id));
}
EOF
```

### Step 3：在页面中使用
```typescript
// frontend/src/app/workspace/page.tsx
import { filterFeaturesByEnvironment } from '@/utils/featureFilter';

const fetchFeatures = async () => {
  try {
    const response: any = await api.features.getAll({ enabled: true });

    if (response.success && response.features) {
      // 根据环境过滤功能
      const filtered = filterFeaturesByEnvironment(response.features);
      setFeatures(filtered);
    }
  } catch (error) {
    console.error('获取功能列表失败:', error);
  }
};
```

### Step 4：验证
```bash
# 启动前端
cd frontend
npm run dev

# 访问 http://localhost:3001/workspace
# 应该看不到"服装清理增强"功能卡片
```

---

## 🎉 效果对比

### 修改前（有问题）
- ❌ 本地环境显示"服装清理增强"功能
- ❌ 用户点击后看到Mock数据（假图片）
- ❌ 体验很差，不知道是测试环境

### 修改后（完美）
- ✅ 本地环境自动隐藏需要生产环境的功能
- ✅ 只显示可以正常测试的功能
- ✅ 避免用户困惑
- ✅ 生产环境自动恢复显示

---

## 📝 需要维护的清单

### 添加新功能时检查
每次添加新功能时，需要判断：

**需要生产环境的功能**（添加到过滤列表）：
- ✅ 使用腾讯云数据万象
- ✅ 使用RunningHub等需要真实API Key的服务
- ✅ 使用微信支付等需要真实凭证的服务

**不需要生产环境的功能**（可以本地测试）：
- ✅ 纯前端功能（素材库、任务列表）
- ✅ 简单CRUD（用户管理、订单管理）
- ✅ 可以Mock的功能

### 更新清单位置
```typescript
// frontend/src/utils/featureFilter.ts
const PRODUCTION_ONLY_FEATURES = [
  'basic_clean',          // 服装清理增强（数据万象）
  'advanced_enhance',     // 高级增强（数据万象）
  'model_pose12',         // AI模特（RunningHub）
  'video_generate',       // 视频生成（需要真实供应商）
  // 添加新功能时在这里更新
];
```

---

## 🚀 总结

**立即可做**：
1. ✅ 前端添加环境变量 `NEXT_PUBLIC_MOCK_MODE=true`
2. ✅ 创建功能过滤工具 `featureFilter.ts`
3. ✅ 在页面中使用过滤函数
4. ✅ 测试验证（本地应该看不到数据万象相关功能）

**长期优化**：
1. 📝 数据库添加 `requires_production` 字段
2. 📝 后端接口自动过滤
3. 📝 前端移除硬编码列表

**维护成本**：
- 每添加一个需要生产环境的功能，在 `PRODUCTION_ONLY_FEATURES` 数组添加一行
- 未来迁移到数据库后，不再需要手动维护

---

**文档创建时间**：2025-10-30
**创建者**：老王（Product Planner Skill）
**适用场景**：本地测试环境功能过滤
