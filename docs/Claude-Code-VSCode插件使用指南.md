# 🚀 Claude Code for VS Code 插件使用指南

> 老王出品！Claude Code 2.0 VS Code 扩展完整使用指南

---

## 📦 Claude Code 2.0 重大更新

### 发布时间：2024-2025

### 核心更新：

1. ✅ **原生 VS Code 扩展**（Beta）
2. ✅ **Sonnet 4.5 驱动**（SWE-bench 77.2% 分数）
3. ✅ **Checkpoints（检查点）**功能
4. ✅ **全新终端 UX**
5. ✅ **实时代码预览**
6. ✅ **子代理（Subagents）**并行开发
7. ✅ **Hooks（钩子）**自动化
8. ✅ **后台任务**支持

---

## 🎯 7 大新功能详解

### 1. 原生 VS Code 扩展（Native VS Code Extension）

**功能**：
- Claude Code 直接集成到 VS Code 侧边栏
- 实时查看 Claude 的代码改动（inline diffs）
- 图形化界面，不用再切换到终端

**使用方法**：
1. 打开 VS Code
2. 点击左侧 Claude Code 图标
3. 在侧边栏中与 Claude 对话
4. 实时看到代码变化

**优势**：
- ⭐⭐⭐⭐⭐ 无需切换窗口
- ⭐⭐⭐⭐⭐ 代码 diff 直接显示
- ⭐⭐⭐⭐⭐ 更直观的 UI

---

### 2. Checkpoints（检查点）

**功能**：
- 保存代码和对话的状态快照
- 随时回滚到之前的状态
- 支持恢复代码、对话或两者

**使用方法**：

#### 创建检查点：
Claude 在关键操作前会自动创建检查点

#### 回滚到检查点：
```
方式 1：按 Esc 两次
方式 2：输入 /rewind 命令
```

#### 选择恢复内容：
- 只恢复代码
- 只恢复对话
- 恢复代码和对话

**使用场景**：
- ✅ Claude 改错了代码，快速回滚
- ✅ 尝试不同的实现方案
- ✅ 保存重要的开发里程碑

**示例**：
```
你："请重构这个函数"
Claude：[修改代码] ← 自动创建 Checkpoint 1
你："不对，改错了"
你：[按 Esc 两次] ← 回滚到 Checkpoint 1
Claude："已回滚到之前的状态"
```

---

### 3. Subagents（子代理）

**功能**：
- Claude 可以启动多个子代理并行工作
- 每个子代理负责专门的任务
- 自动协调和合并结果

**使用场景**：
```
你："同时开发用户管理的前端和后端"

Claude：
  ├── Subagent 1：开发后端 API（Express.js）
  ├── Subagent 2：开发前端页面（Next.js）
  └── 主 Agent：协调和合并代码
```

**优势**：
- ⭐⭐⭐⭐⭐ 并行开发，速度更快
- ⭐⭐⭐⭐⭐ 专业分工，质量更高
- ⭐⭐⭐⭐⭐ 自动合并，无冲突

**示例**：
```
请同时完成以下任务：
1. 后端：开发用户登录 API
2. 前端：开发登录页面
3. 测试：编写 E2E 测试

Claude 会启动 3 个 Subagents 并行工作
```

---

### 4. Hooks（钩子）

**功能**：
- 在特定事件发生时自动触发动作
- 自动化重复性任务
- 自定义开发工作流

**常见 Hooks**：
- `pre-commit`：提交前自动运行测试
- `post-write`：写文件后自动格式化
- `pre-read`：读文件前自动备份

**使用方法**：

在项目根目录创建 `.claude/hooks/` 目录：

```bash
mkdir -p .claude/hooks
```

创建 Hook 脚本：

```bash
# .claude/hooks/pre-commit.sh
#!/bin/bash
echo "运行测试..."
npm test
```

**配置 Hooks**：

```json
// .claude/settings.json
{
  "hooks": {
    "pre-commit": ".claude/hooks/pre-commit.sh",
    "post-write": "npx prettier --write"
  }
}
```

**示例**：
```
你："帮我实现用户登录功能"
Claude：[开始编码]
  → [写文件] → 触发 post-write Hook → 自动格式化代码
  → [完成] → 触发 pre-commit Hook → 自动运行测试
```

---

### 5. 后台任务（Background Tasks）

**功能**：
- 长时间运行的任务在后台执行
- 不阻塞主对话
- 实时查看任务状态

**使用场景**：
- 运行测试套件（5-10 分钟）
- 构建项目（长时间编译）
- 数据库迁移
- 部署脚本

**使用方法**：

```
你："请在后台运行完整测试套件"
Claude："已启动后台任务 #1234"
你："继续开发其他功能"
Claude：[在后台运行测试，不影响当前对话]

// 稍后
你："检查后台任务 #1234 的状态"
Claude："测试完成，通过率 95%"
```

**命令**：
```
/background run npm test          # 启动后台任务
/background list                  # 查看所有后台任务
/background status 1234           # 查看任务状态
/background kill 1234             # 终止任务
```

---

### 6. Tab 切换思考模式

**功能**：
- 按 Tab 键切换 Claude 的思考过程显示
- 查看 Claude 的推理过程
- 理解 Claude 为什么这么做

**使用方法**：
1. Claude 回复时按 `Tab` 键
2. 显示/隐藏 Claude 的内部思考过程

**示例**：
```
你："为什么选择用 Redis 而不是 Memcached？"
Claude：[按 Tab 显示思考过程]

💭 思考过程：
1. 分析场景：需要持久化、数据结构支持
2. Redis 支持 List/Set/Hash，Memcached 只支持 String
3. Redis 支持持久化（RDB/AOF）
4. 结论：Redis 更适合

回答：我选择 Redis 是因为...
```

---

### 7. /usage 命令

**功能**：
- 查看 Token 使用情况
- 监控成本
- 优化 Prompt

**使用方法**：
```
/usage                    # 查看当前对话 Token 使用
/usage --session          # 查看本次会话总使用
/usage --today            # 查看今日总使用
/usage --billing          # 查看计费信息
```

**输出示例**：
```
📊 Token 使用情况

本次对话：
- Input Tokens:  12,345
- Output Tokens: 8,901
- Total:         21,246
- 成本: $0.42

本次会话总计：
- Total Tokens:  156,789
- 成本: $3.14
```

---

## 🔧 安装和配置

### 安装方法

#### 方式 1：VS Code Marketplace（推荐）

1. 打开 VS Code
2. 点击左侧扩展图标（或按 `Ctrl+Shift+X`）
3. 搜索 "Claude Code"
4. 点击 "Install"

#### 方式 2：命令行安装

```bash
code --install-extension anthropic.claude-code
```

#### 方式 3：手动下载

1. 访问：https://marketplace.visualstudio.com/items?itemName=anthropic.claude-code
2. 点击 "Download Extension"
3. 在 VS Code 中 → Extensions → "..." → Install from VSIX

---

### 配置 API Key

1. 打开 VS Code
2. 按 `Ctrl+Shift+P`（或 `Cmd+Shift+P`）
3. 输入 "Claude Code: Set API Key"
4. 粘贴你的 Anthropic API Key

**或者**手动编辑配置文件：

```json
// settings.json
{
  "claudeCode.apiKey": "your-api-key-here",
  "claudeCode.model": "claude-sonnet-4-5-20250929"
}
```

---

### 高级配置

```json
// .vscode/settings.json
{
  // API 配置
  "claudeCode.apiKey": "${env:ANTHROPIC_API_KEY}",
  "claudeCode.model": "claude-sonnet-4-5-20250929",

  // 功能开关
  "claudeCode.enableCheckpoints": true,
  "claudeCode.enableSubagents": true,
  "claudeCode.enableHooks": true,

  // Checkpoints 配置
  "claudeCode.checkpoints.autoCreate": true,
  "claudeCode.checkpoints.maxCount": 10,

  // Subagents 配置
  "claudeCode.subagents.maxConcurrent": 3,

  // 显示配置
  "claudeCode.showThinking": false,
  "claudeCode.inlineDiff": true
}
```

---

## 🎯 实战使用指南

### 场景 1：快速开发 API

```
你：请使用 backend-dev skill 开发用户登录 API
    - POST /api/v1/auth/login
    - JWT 认证

Claude：
  1. 创建 Checkpoint
  2. 设计 OpenAPI 契约
  3. 实现代码（实时显示 diff）
  4. 编写测试
  5. 在后台运行测试
  6. 完成后自检

你：[实时在 VS Code 侧边栏看到代码变化]
```

---

### 场景 2：并行开发前后端

```
你：同时开发用户管理的前后端

Claude：
  ├── Subagent 1（后端）：
  │   ├── 设计 API
  │   ├── 实现路由/控制器
  │   └── 编写测试
  │
  ├── Subagent 2（前端）：
  │   ├── 设计页面
  │   ├── 实现组件
  │   └── 编写测试
  │
  └── 主 Agent：协调和合并

完成时间：传统 30 分钟 → 并行 15 分钟
```

---

### 场景 3：使用 Checkpoints 尝试多种方案

```
你：请重构这个函数，提升性能

Claude：[方案 A：使用缓存] ← Checkpoint 1
你：试试其他方案

Claude：[方案 B：使用算法优化] ← Checkpoint 2
你：对比两个方案

你：/rewind → 回到 Checkpoint 1
    选择方案 A
```

---

### 场景 4：自动化工作流（Hooks）

```
配置 Hooks：
  pre-commit  → 运行 ESLint + Prettier
  post-write  → 自动格式化
  pre-push    → 运行完整测试套件

你："实现用户注册功能"
Claude：
  [写代码] → post-write Hook → 自动格式化
  [完成] → pre-commit Hook → ESLint 检查
  [你执行 git push] → pre-push Hook → 运行测试

全程自动化，质量保障！
```

---

## 📊 版本对比

| 功能 | Claude Code 1.0 | Claude Code 2.0 |
|-----|----------------|----------------|
| **界面** | 终端 CLI | VS Code 原生扩展 |
| **模型** | Sonnet 3.5 | Sonnet 4.5 ⚡ |
| **SWE-bench** | 49% | 77.2% 🚀 |
| **Checkpoints** | ❌ | ✅ |
| **Subagents** | ❌ | ✅ |
| **Hooks** | ❌ | ✅ |
| **后台任务** | ❌ | ✅ |
| **实时 Diff** | ❌ | ✅ |
| **思考模式** | ❌ | ✅ (Tab 切换) |

---

## 🆚 Claude Code vs 其他工具

| 工具 | 代码生成 | 自主操作 | 多文件编辑 | 测试 | 部署 |
|-----|---------|---------|-----------|------|------|
| **Claude Code 2.0** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| GitHub Copilot | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ❌ |
| Cursor | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| Tabnine | ⭐⭐⭐ | ⭐ | ⭐⭐ | ⭐ | ❌ |

---

## 💡 使用技巧

### 技巧 1：善用 Checkpoints

```
开发新功能前：
  → 创建 Checkpoint（自动）
  → 尝试实现
  → 不满意？按 Esc 两次回滚
  → 尝试其他方案
```

### 技巧 2：并行加速开发

```
单独开发：30 分钟
  ↓
启用 Subagents：15 分钟
  前端 + 后端 + 测试 并行
```

### 技巧 3：配置 Hooks 自动化

```
.claude/hooks/pre-commit.sh:
  #!/bin/bash
  npm run lint
  npm test
  npm run build

永远不会提交破损代码！
```

### 技巧 4：结合 Skills 使用

```
你："请使用 backend-dev skill 开发登录 API"

Claude Code 2.0：
  ✅ 加载 Skills 规范
  ✅ 创建 Checkpoint
  ✅ 实时显示代码变化
  ✅ 在后台运行测试
  ✅ 触发 pre-commit Hook

完美配合！
```

---

## 🐛 常见问题

### Q1：VS Code 扩展安装后找不到？

**解决方案**：
1. 重启 VS Code
2. 按 `Ctrl+Shift+P` → "Claude Code: Activate"
3. 检查左侧边栏是否有 Claude Code 图标

---

### Q2：API Key 配置无效？

**解决方案**：
```bash
# 方式 1：环境变量
export ANTHROPIC_API_KEY="your-key"

# 方式 2：配置文件
{
  "claudeCode.apiKey": "your-key"
}

# 方式 3：命令面板
Ctrl+Shift+P → "Claude Code: Set API Key"
```

---

### Q3：Checkpoints 不生效？

**解决方案**：
```json
// settings.json
{
  "claudeCode.enableCheckpoints": true,
  "claudeCode.checkpoints.autoCreate": true
}
```

---

### Q4：Subagents 没有并行执行？

**解决方案**：
```json
// settings.json
{
  "claudeCode.enableSubagents": true,
  "claudeCode.subagents.maxConcurrent": 3
}
```

---

## 🚀 最佳实践

### 1. 开发新功能

```
步骤 1：使用 Product Planner Skill 拆分任务
步骤 2：启用 Checkpoints
步骤 3：并行开发（Subagents）
步骤 4：自动测试（Hooks）
步骤 5：Code Review（Reviewer Skill）
步骤 6：部署（CodeBuddy Deploy Skill）
```

### 2. 代码重构

```
步骤 1：创建 Checkpoint
步骤 2：尝试重构方案 A
步骤 3：按 Tab 查看 Claude 的思考过程
步骤 4：不满意？Esc 两次回滚
步骤 5：尝试重构方案 B
步骤 6：对比两个方案，选择最优
```

### 3. Bug 修复

```
步骤 1：使用 Reviewer Skill 定位问题
步骤 2：创建 Checkpoint
步骤 3：修复 Bug
步骤 4：后台运行测试
步骤 5：测试通过 → 提交
步骤 6：测试失败 → 回滚 Checkpoint
```

---

## 📞 获取帮助

- **官方文档**：https://docs.claude.com/en/docs/claude-code/vs-code
- **VS Code Marketplace**：https://marketplace.visualstudio.com/items?itemName=anthropic.claude-code
- **GitHub Issues**：https://github.com/anthropics/claude-code/issues
- **社区论坛**：https://community.anthropic.com/

---

## 🎉 总结

### Claude Code 2.0 for VS Code 的核心优势：

✅ **原生 VS Code 集成**：无需切换窗口
✅ **Sonnet 4.5 驱动**：77.2% SWE-bench 分数
✅ **Checkpoints**：随时回滚，大胆尝试
✅ **Subagents**：并行开发，速度翻倍
✅ **Hooks**：自动化工作流
✅ **后台任务**：长时间任务不阻塞
✅ **实时 Diff**：代码变化一目了然

### 与 AI Dev Skills 完美配合：

```
Claude Code 2.0 + AI Dev Skills = 🚀🚀🚀

开发效率：10x 提升
代码质量：80%+ 覆盖率
开发体验：丝滑流畅
```

---

艹！Claude Code 2.0 太强了！赶紧试试吧！💪
