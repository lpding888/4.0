# 任务卡：本地开发环境启动

## 📋 任务信息

- **任务名称**：本地开发环境启动
- **任务类型**：环境配置
- **优先级**：P0（最高优先级）
- **预计耗时**：30分钟
- **负责技能包**：`codebuddy_deploy_skill`（部署运维工程师）
- **协助技能包**：
  - `backend_dev_skill`（后端环境配置支持）
  - `frontend_dev_skill`（前端环境配置支持）
- **创建时间**：2025-10-30
- **创建者**：老王（Product Planner Skill）

---

## 👥 角色分工

### 主要负责：`codebuddy_deploy_skill`（部署运维工程师）
**职责**：
- ✅ 负责Docker容器启动和管理
- ✅ 负责环境变量配置和验证
- ✅ 负责服务启动和健康检查
- ✅ 负责常见问题排查和解决
- ✅ 负责编写部署脚本和自动化工具

**为什么选它？**
- 部署运维工程师专门负责环境搭建、服务启动、配置管理
- 最熟悉Docker、环境变量、服务部署流程
- 能快速定位和解决环境问题

### 协助角色：

#### `backend_dev_skill`（后端开发工程师）
**协助内容**：
- 提供后端环境变量配置说明
- 解释数据库迁移逻辑
- 帮助排查后端服务启动问题

#### `frontend_dev_skill`（前端开发工程师）
**协助内容**：
- 提供前端环境变量配置说明
- 解释前端构建流程
- 帮助排查前端页面加载问题

---

## 🎯 任务目标

在本地搭建完整的前后端开发环境，能够正常运行和测试项目。

**验收标准**：
- ✅ Docker容器（MySQL + Redis + MinIO）启动成功
- ✅ 后端API服务启动成功（http://localhost:3000）
- ✅ 前端Web应用启动成功（http://localhost:3001）
- ✅ 能访问登录页面并成功登录
- ✅ 能访问工作台查看功能卡片

---

## 🛠️ 前置条件

### 必须安装的软件
- [x] **Docker Desktop**（Windows/Mac）
  - 下载地址：https://www.docker.com/products/docker-desktop
  - 确保Docker已启动并运行

- [x] **Node.js 18+**
  - 下载地址：https://nodejs.org/
  - 验证：`node --version`（应显示v18.x.x或更高）

- [x] **npm 9+**
  - 通常随Node.js一起安装
  - 验证：`npm --version`（应显示9.x.x或更高）

### 确认系统环境
```bash
# 检查Docker
docker --version
docker ps

# 检查Node.js
node --version

# 检查npm
npm --version
```

---

## 📝 详细执行步骤

### 第一阶段：启动Docker容器（10分钟）

#### 步骤1：启动Docker服务

```bash
# 在项目根目录执行
cd "c:\Users\qq100\Desktop\迭代目录\新建文件夹 (4)"

# 启动MySQL、Redis、MinIO容器
docker-compose -f docker-compose.dev.yml up -d
```

**预期输出**：
```
Creating ai-photo-mysql-dev ... done
Creating ai-photo-redis-dev ... done
Creating ai-photo-minio-dev ... done
```

#### 步骤2：验证容器状态

```bash
# 查看容器状态
docker-compose -f docker-compose.dev.yml ps
```

**预期输出**：所有容器状态都是`healthy`或`running`

```
NAME                  STATUS              PORTS
ai-photo-mysql-dev    Up (healthy)        0.0.0.0:3306->3306/tcp
ai-photo-redis-dev    Up (healthy)        0.0.0.0:6379->6379/tcp
ai-photo-minio-dev    Up (healthy)        0.0.0.0:9000->9000/tcp, 0.0.0.0:9001->9001/tcp
```

#### 步骤3：等待MySQL健康检查通过

```bash
# 查看MySQL日志，确认启动完成
docker-compose -f docker-compose.dev.yml logs mysql

# 看到这行说明MySQL已准备好
# [Server] /usr/sbin/mysqld: ready for connections
```

⏰ **等待10-15秒让MySQL完全启动！**

---

### 第二阶段：配置并启动后端（10分钟）

#### 步骤4：配置后端环境变量

```bash
cd backend

# 复制Docker开发环境配置
cp .env.docker .env

# 验证配置文件
cat .env | grep DB_HOST
```

**预期输出**：
```
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=dev_password_123
DB_NAME=ai_photo
```

#### 步骤5：安装后端依赖

```bash
# 在backend目录
npm install
```

**预期耗时**：2-3分钟

**可能遇到的问题**：
- 如果npm install很慢，可以使用国内镜像：
  ```bash
  npm config set registry https://registry.npmmirror.com
  npm install
  ```

#### 步骤6：执行数据库迁移

```bash
# 在backend目录
npm run db:migrate
```

**预期输出**：
```
Batch 1 run: 15 migrations
✓ 20241027000001_create_users_table.js
✓ 20241027000002_create_orders_table.js
✓ 20241027000003_create_tasks_table.js
✓ 20241027000004_create_verification_codes_table.js
✓ 20241029000001_create_feature_definitions_table.js
✓ 20241029000002_create_form_schemas_table.js
✓ 20241029000003_create_pipeline_schemas_table.js
✓ 20241029000004_extend_tasks_table.js
✓ 20241029000005_create_task_steps_table.js
✓ 20241029000006_create_provider_endpoints_table.js
✓ 20241029000007_create_provider_health_table.js
✓ 20241029000008_create_assets_table.js
✓ 20251029110001_create_distributors_table.js
✓ 20251029110002_create_referral_relationships_table.js
✓ 20251029110003_create_commissions_table.js
```

#### 步骤7：启动后端服务

```bash
# 在backend目录
npm run dev
```

**预期输出**：
```
🚀 后端服务启动在: http://localhost:3000
📊 数据库连接成功: ai_photo@localhost:3306
🔥 Redis连接成功: localhost:6379
✨ MinIO对象存储就绪: http://localhost:9000
```

**⚠️ 保持这个终端窗口打开！**

---

### 第三阶段：配置并启动前端（10分钟）

#### 步骤8：打开新终端窗口配置前端

```bash
# 打开新的终端窗口
cd "c:\Users\qq100\Desktop\迭代目录\新建文件夹 (4)\frontend"

# 创建本地环境变量文件
cat > .env.local << 'EOF'
# 本地开发环境配置
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_COS_BUCKET=ai-photo-dev
NEXT_PUBLIC_COS_REGION=local
NEXT_PUBLIC_COS_IMAGE_DOMAIN=http://localhost:9000
EOF
```

**验证配置**：
```bash
cat .env.local
```

#### 步骤9：安装前端依赖

```bash
# 在frontend目录
npm install
```

**预期耗时**：3-5分钟

#### 步骤10：启动前端服务

```bash
# 在frontend目录
npm run dev
```

**预期输出**：
```
▲ Next.js 14.x
- Local:        http://localhost:3001
- Ready in 2.5s

✓ Compiled in 2.5s
```

**⚠️ 保持这个终端窗口打开！**

---

## ✅ 验收测试

### 测试1：后端API健康检查

打开浏览器或使用curl：
```bash
curl http://localhost:3000/api/health
```

**预期返回**：
```json
{
  "status": "ok",
  "timestamp": "2025-10-30T12:00:00.000Z",
  "database": "connected",
  "redis": "connected"
}
```

### 测试2：前端页面访问

打开浏览器访问：http://localhost:3001

**预期结果**：
- ✅ 页面正常加载
- ✅ 能看到登录/注册页面
- ✅ 没有控制台错误

### 测试3：登录功能测试

1. 访问：http://localhost:3001/login
2. 输入手机号：`13800138000`
3. 输入验证码：`123456`（本地开发环境任意6位数字都可以）
4. 点击登录

**预期结果**：
- ✅ 登录成功
- ✅ 跳转到工作台页面
- ✅ 能看到功能卡片

### 测试4：工作台功能卡片

访问：http://localhost:3001/workspace

**预期结果**：
- ✅ 能看到"服装清理增强"功能卡
- ✅ 能看到"AI模特12分镜"功能卡
- ✅ 能看到会员状态和配额信息

### 测试5：管理后台访问

访问：http://localhost:3001/admin

**预期结果**：
- ✅ 能访问管理后台
- ✅ 能看到用户列表
- ✅ 能看到任务列表

---

## 🎯 完成标志

当你完成以下所有检查项，说明本地开发环境启动成功：

- [x] Docker容器全部running且healthy
- [x] 后端服务启动成功（http://localhost:3000）
- [x] 前端服务启动成功（http://localhost:3001）
- [x] 能成功登录系统
- [x] 能访问工作台看到功能卡片
- [x] 能访问管理后台

**🎉 恭喜！你的本地开发环境已经就绪！**

---

## 🐛 常见问题排查

### 问题1：Docker容器启动失败

**症状**：`docker-compose up -d`报错

**解决方案**：
```bash
# 检查Docker是否运行
docker ps

# 检查端口是否被占用
netstat -ano | findstr :3306
netstat -ano | findstr :6379
netstat -ano | findstr :9000

# 如果端口被占用，修改docker-compose.dev.yml中的端口映射
# 例如：将"3306:3306"改为"3307:3306"
```

### 问题2：MySQL连接失败

**症状**：后端启动报错"ECONNREFUSED 127.0.0.1:3306"

**解决方案**：
```bash
# 等待MySQL完全启动（10-15秒）
docker-compose -f docker-compose.dev.yml logs mysql

# 查看MySQL是否healthy
docker ps

# 手动测试MySQL连接
docker exec -it ai-photo-mysql-dev mysql -uroot -pdev_password_123 -e "SELECT 1"
```

### 问题3：数据库迁移失败

**症状**：`npm run db:migrate`报错

**解决方案**：
```bash
# 检查.env配置
cat backend/.env

# 回滚迁移
npm run db:rollback

# 重新执行迁移
npm run db:migrate
```

### 问题4：前端无法连接后端

**症状**：前端页面显示"网络错误"

**解决方案**：
```bash
# 检查后端是否启动
curl http://localhost:3000/api/health

# 检查前端.env.local配置
cat frontend/.env.local

# 确认NEXT_PUBLIC_API_URL=http://localhost:3000/api

# 重启前端服务
# Ctrl+C停止，然后重新npm run dev
```

### 问题5：npm install很慢

**解决方案**：
```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 清除缓存重试
npm cache clean --force
npm install
```

### 问题6：端口冲突

**症状**：前端启动提示"Port 3000 is already in use"

**解决方案**：
```bash
# 修改前端端口
# 在frontend目录执行
npm run dev -- -p 3001

# 或者修改package.json的dev脚本：
# "dev": "next dev -p 3001"
```

---

## 📊 环境信息

### 后端服务
- **地址**：http://localhost:3000
- **API路径**：http://localhost:3000/api
- **健康检查**：http://localhost:3000/api/health

### 前端服务
- **地址**：http://localhost:3001
- **登录页**：http://localhost:3001/login
- **工作台**：http://localhost:3001/workspace
- **管理后台**：http://localhost:3001/admin

### Docker容器
| 服务 | 容器名 | 端口 | 用户名 | 密码 |
|------|--------|------|--------|------|
| MySQL | ai-photo-mysql-dev | 3306 | root | dev_password_123 |
| Redis | ai-photo-redis-dev | 6379 | - | - |
| MinIO | ai-photo-minio-dev | 9000(API), 9001(Web) | minioadmin | minioadmin123 |

### 数据库信息
- **主机**：localhost
- **端口**：3306
- **数据库**：ai_photo
- **用户**：root
- **密码**：dev_password_123

### MinIO控制台
- **地址**：http://localhost:9001
- **用户名**：minioadmin
- **密码**：minioadmin123

---

## 🔄 日常开发流程

### 启动开发环境

```bash
# 1. 启动Docker容器
docker-compose -f docker-compose.dev.yml up -d

# 2. 启动后端（终端1）
cd backend && npm run dev

# 3. 启动前端（终端2）
cd frontend && npm run dev
```

### 停止开发环境

```bash
# 停止后端和前端：Ctrl+C

# 停止Docker容器
docker-compose -f docker-compose.dev.yml down

# 停止并删除数据（完全清理）
docker-compose -f docker-compose.dev.yml down -v
```

### 查看日志

```bash
# 查看后端日志
cd backend && npm run dev

# 查看Docker容器日志
docker-compose -f docker-compose.dev.yml logs -f

# 查看MySQL日志
docker-compose -f docker-compose.dev.yml logs mysql

# 查看Redis日志
docker-compose -f docker-compose.dev.yml logs redis
```

### 数据库操作

```bash
# 连接MySQL
docker exec -it ai-photo-mysql-dev mysql -uroot -pdev_password_123 ai_photo

# 常用SQL
SHOW TABLES;
SELECT * FROM users;
SELECT * FROM tasks;
SELECT * FROM orders;

# 清空测试数据
TRUNCATE TABLE tasks;
TRUNCATE TABLE verification_codes;
```

---

## 📚 相关文档

- [项目README](../README.md)
- [后端开发指南](../backend/DEV_SETUP.md)
- [API文档](./API对接指南-小程序和APP.md)
- [数据库Schema](../backend/src/db/migrations/)

---

## ✅ 任务检查清单

在完成任务前，请确认以下所有项目：

### 环境准备
- [ ] Docker Desktop已安装并启动
- [ ] Node.js 18+已安装
- [ ] npm 9+已安装

### Docker容器
- [ ] MySQL容器启动成功（healthy）
- [ ] Redis容器启动成功（healthy）
- [ ] MinIO容器启动成功（healthy）
- [ ] 能连接到MySQL数据库
- [ ] 能访问MinIO控制台

### 后端服务
- [ ] .env.docker已复制为.env
- [ ] npm install成功
- [ ] 数据库迁移成功
- [ ] 后端服务启动成功
- [ ] http://localhost:3000/api/health返回正常

### 前端服务
- [ ] .env.local已创建并配置正确
- [ ] npm install成功
- [ ] 前端服务启动成功
- [ ] http://localhost:3001可以访问

### 功能测试
- [ ] 能访问登录页面
- [ ] 能成功登录（任意手机号+6位数字）
- [ ] 能访问工作台看到功能卡片
- [ ] 能访问管理后台
- [ ] 浏览器控制台无报错

---

## 🎉 任务完成

当以上所有检查项都完成后，本地开发环境启动任务完成！

**下一步**：
- [ ] 开始功能开发
- [ ] 编写单元测试
- [ ] 提交代码到develop分支

---

## 🔄 任务执行方式

### 方式1：AI代理执行（推荐）

如果你有AI编程助手（Claude Code/Cursor/GitHub Copilot等），可以直接把这个任务卡交给对应的技能包执行：

```bash
# 启动部署运维技能包
@codebuddy_deploy_skill 请按照任务卡执行本地开发环境启动
```

AI代理会：
1. 自动执行所有命令
2. 验证每个步骤的结果
3. 遇到问题自动排查
4. 最后给出完整的验收报告

### 方式2：手动执行

如果你想手动执行，按照上面的详细步骤一步一步做即可。

---

## 📞 遇到问题找谁？

### 技能包求助通道

如果你在执行过程中遇到问题，可以向对应的技能包求助：

| 问题类型 | 求助技能包 | 说明 |
|---------|-----------|------|
| Docker容器问题 | `codebuddy_deploy_skill` | 容器启动、端口冲突、健康检查 |
| MySQL连接问题 | `backend_dev_skill` | 数据库连接、迁移失败 |
| 后端服务问题 | `backend_dev_skill` | API启动失败、环境变量错误 |
| 前端构建问题 | `frontend_dev_skill` | 依赖安装、构建失败、页面加载 |
| 性能问题 | `qa_acceptance_skill` | 启动慢、占用资源高 |

### 求助格式

```bash
@技能包名称 我遇到了问题：[具体问题描述]

错误信息：
[粘贴错误日志]

已尝试的解决方案：
[你已经尝试过的方法]
```

---

**任务创建时间**：2025-10-30
**最后更新时间**：2025-10-30
**任务状态**：待执行
**负责技能包**：`codebuddy_deploy_skill`
**创建者**：老王（Product Planner Skill）
