好的——基于你已确定采用**方案A（完全自研）**，下面给出一份可落地、可交给 AI 编程工具直接执行的实施规划。内容覆盖：技术架构、数据库与缓存策略、工作量与时间表、风险与应对，以及**30+ 可执行任务卡（含 AI Prompt）**。全程围绕你现有栈（Next.js 14 + Express/Knex + MySQL + Redis + COS + PM2，单机 4c4g）与“1 人 + AI 工具高效实现”的约束来设计。

---

# 总体蓝图（摘要）

* **总体思路**：后端以**配置即代码（Config-as-Code）**为中心，提供5大域：Provider 管理、表单设计器、流程编排、Prompt 中心、内容配置。
* **核心突破**：

  1. Provider 动态加载机制 + AES 密钥管理 + 白名单沙箱；
  2. 表单设计器自研（react-hook-form + dnd-kit + zod），满足 10 类字段；
  3. 流程编排器基于 React Flow 自定义节点，完善的合法性校验（DAG 检测、参数校验、依赖分析）；
  4. Prompt 管理采用 **Handlebars**（支持条件/循环）+ Monaco，内置变量提示与实时预览；
  5. **Redis 缓存 + Pub/Sub 广播**，做到“更新即生效、无需重启”；本地 JSON 快照做降级托底。
* **MVP（2 周）**：Provider 动态加载、表单设计器（10 类字段 + Schema 生/读/写 + 预览）、流程编排器（节点/边/校验 + 测试运行）、Prompt 基础（编辑+渲染预览）、公告与轮播最小集、缓存与降级闭环、E2E 关键流。

---

# 一、技术架构设计

## 1) 组件视图与调用链（简图）

```
[Next.js Admin (AntD)]
   ├─ Feature Wizard（表单设计器 + 流程编排器 + 预览/发布）
   ├─ Provider 管理（CRUD/连通性测试）
   ├─ Prompt 中心（Monaco + Handlebars 预览/测试）
   └─ 内容配置（公告/轮播/套餐/文案）

[Express + Knex + MySQL + Redis]
   ├─ Config Service（DB <-> Redis 缓存，Pub/Sub 广播）
   ├─ Provider Registry（白名单 + 动态加载 + AES 凭证解密）
   ├─ Pipeline Service（DAG 校验/执行/可视化日志）
   ├─ Prompt Engine（Handlebars 渲染 + 测试调用）
   └─ Content Service（公告/轮播/套餐/文案 CRUD）

[执行面]
   ├─ 已有 Pipeline 引擎（SYNC_IMAGE_PROCESS / RUNNINGHUB_WORKFLOW / SCF_POST_PROCESS）
   └─ COS（素材/轮播图等）  |  PM2（3 进程）
```

---

## 2) 表单设计器技术方案

* **推荐方案：自研轻量栈**

  * **react-hook-form**（性能优、受控/非受控灵活）+ **zod**（类型安全校验）
  * **dnd-kit** 实现字段拖拽/排序/复制
  * AntD 作为基础控件库（Form.Item/Drawer/Modal/ProLayout）
  * 好处：更轻、可深度定制、和现有前端最佳匹配；避免 Form.io 的重量与学习/定制成本
  * 风险：自研工作量略高 → 通过任务卡拆小步快跑，先覆盖 10 类字段最小集

* **Form.io 方案对比**

  * 👍 快速出原型、内置丰富组件；👎 体积大、定制深度/风格统一成本较高、以后迁移约束强。
  * **结论**：保持可插拔接口（预留 “外部表单引擎” 适配层），当前优先自研。

* **表单 JSON Schema（示意，覆盖 10 类字段）**

```json
{
  "id": "form_2025_001",
  "title": "AI模特生成参数",
  "version": 1,
  "fields": [
    {"key":"clothImage","label":"服装图片","type":"image","required":true,"accept":["image/*"],"help":"上传正面平铺图"},
    {"key":"title","label":"标题","type":"text","maxLength":60,"placeholder":"请输入标题"},
    {"key":"desc","label":"描述","type":"textarea","maxLength":300},
    {"key":"price","label":"价格","type":"number","min":0,"step":0.01,"default":0},
    {"key":"style","label":"风格","type":"select","options":[{"label":"街拍","value":"street"},{"label":"简约","value":"simple"}]},
    {"key":"gender","label":"性别","type":"radio","options":[{"label":"女","value":"f"},{"label":"男","value":"m"}],"default":"f"},
    {"key":"tags","label":"标签","type":"checkbox","options":[{"label":"通勤","value":"work"},{"label":"运动","value":"sport"}]},
    {"key":"strength","label":"强度","type":"slider","min":0,"max":10,"default":5},
    {"key":"themeColor","label":"主色调","type":"color","default":"#222222"},
    {"key":"shootDate","label":"拍摄日期","type":"date"}
  ],
  "validation": {"mode":"zod"},
  "ui": {"layout":"one-column"}
}
```

* **关键点**

  * 字段元数据**唯一 key**、**类型安全**、**双端校验复用**（zod schema 通过 swagger/openapi + zod-to-ts 输出）
  * **实时预览**：同 schema 渲染出“预览表单” + 默认值/校验即时反馈
  * **持久化**：`form_schemas` version 字段 + 发布状态（draft/published）

---

## 3) Pipeline 流程编辑器（React Flow）

* **节点类型**：`API_CALL`、`SCF_FUNCTION`、`CONDITION`、`PARALLEL`、`JOIN`、`END`
* **UI 设计**：**侧边栏（Drawer）编辑节点属性**更利于保持画布空间与上下文可视；新增/选中节点时打开
* **合法性校验（核心）**

  * DAG 检测（无环约束）：拓扑排序/DFS
  * **入度/出度规则**：如 `JOIN` 入度 ≥2、`END` 出度 = 0
  * **参数域校验**：节点所需参数是否由上游输出或来自表单/常量
  * **并行组**：`PARALLEL` 子分支必须最终汇聚到 `JOIN`
* **Pipeline JSON Schema（示意）**

```json
{
  "id":"pipe_2025_001",
  "name":"AI模特12分镜",
  "version":1,
  "nodes":[
    {"id":"n1","type":"API_CALL","name":"去背景","providerRef":"tencent_ci","params":{"path":"/ci/bgremove","timeout":15}},
    {"id":"n2","type":"CONDITION","expr":"${form.style}==='street'"},
    {"id":"n3","type":"PARALLEL","name":"多角度生成"},
    {"id":"n3a","type":"API_CALL","name":"角度A","providerRef":"runninghub","params":{"workflowId":"xA"}},
    {"id":"n3b","type":"API_CALL","name":"角度B","providerRef":"runninghub","params":{"workflowId":"xB"}},
    {"id":"n4","type":"JOIN","name":"聚合"},
    {"id":"n5","type":"SCF_FUNCTION","name":"后处理","providerRef":"scf","params":{"func":"postEnhance"}},
    {"id":"n6","type":"END","name":"结束"}
  ],
  "edges":[
    {"from":"n1","to":"n2"},
    {"from":"n2","to":"n3","when":true},
    {"from":"n3","to":"n3a"},{"from":"n3","to":"n3b"},
    {"from":"n3a","to":"n4"},{"from":"n3b","to":"n4"},
    {"from":"n4","to":"n5"},{"from":"n5","to":"n6"}
  ],
  "io":{
    "inputs":["clothImage","style","strength"],
    "outputs":["images[]","video?","log"]
  }
}
```

* **合法性检测（伪代码概念）**

```
validate(pipeline):
  ensureSingleEnd()
  ensureNoCyclesByDFS()
  for node in nodes:
    ruleCheck(node)  // in/out-degree, type rules
    paramCheck(node) // params present, type, source
  ensureParallelJoinPairs()
```

---

## 4) Provider 动态加载机制

* **现状问题**：`pipelineEngine.service.js` 中 provider 映射硬编码
* **改造目标**：**ProviderLoader** 按 `provider_endpoints` 配置动态加载，对外暴露统一接口
* **安全基线**

  * 只允许加载在**白名单目录**（如 `backend/src/providers/impl`）内的类
  * DB 中仅存 **handler key**（如 `"TencentCIProvider"`），不存任意路径
  * **AES-256-CBC** 加密敏感字段（密钥、token），主密钥仅来自环境变量，支持轮换
  * **沙箱**：不允许 `child_process` 等危险能力；仅暴露 HTTP/gRPC 客户端
* **IProvider（TS 接口，抽象）**

  * `validate(params): void | throws`
  * `execute(params, ctx): Promise<ProviderResult>`
  * `health(): Promise<HealthStatus>`（可选）
* **ProviderLoader（思路）**

  * 维护 `handlerKey -> class` 的**注册表**（启动时扫描白名单目录或静态 import 注册）
  * **缓存** 已实例化 Provider（key + endpointId）
  * 从 `provider_endpoints` 读配置 → 解密凭证 → 构造实例 → 缓存
* **provider_endpoints** 表新增字段

  * `handler_key`（varchar, 索引）、`auth`（json）、`status`、`last_called_at`、`deleted_at`（软删）
  * 复合索引：(`handler_key`,`status`)、(`name`,`type`)

---

## 5) Prompt 变量替换引擎

* **技术选型**：**Handlebars.js**

  * 👍 支持 `{{#if}} / {{#each}} / partial`，表达性强；生态成熟；与 Monaco 易整合
  * 相比 mustache（无逻辑）与正则（脆弱），更适合后续复杂模板
* **Monaco 集成**

  * 自定义语言配置：将 `{{ }}` 变量高亮、提示来自 `variables` 元数据
  * 实时预览：左侧编辑、右侧渲染（示例变量 JSON）
* **在线测试**

  * 输入变量 → 本地渲染 → 可选调用指定 Provider 真实请求（带风控开关与速率限制）

---

## 6) 前端动态表单渲染优化

* **react-hook-form 优势**：订阅粒度更细，渲染次数少，搭配 zodResolver 复用校验
* **大表单优化**：

  * 虚拟滚动（如 50+ 字段时）、分组折叠、分步表单
  * 异步校验节流、图片/大字段懒加载
  * 设计器与预览分离渲染容器，减少重排重绘
* **双端校验**：前端 zod → 后端 zod/yup 等效校验，**错误码统一**，避免“前端过/后端挂”

---

# 二、数据库设计评审与缓存

## 7) 新表评审与改进建议

* **announcements**

  * 建议补充：`target_filters JSON`（如地区/版本/渠道）、`priority TINYINT`、`creator_id`、`updated_at`、`deleted_at`
  * 索引：(`status`,`publish_at`)、(`target_audience`)、(`priority`)
* **banners**

  * 建议补充：`image_key`（COS 对象键，便于迁移）、`creator_id`、`updated_at`、`deleted_at`
  * 索引：(`status`,`publish_at`), (`sort_order`)
* **membership_plans / membership_benefits**

  * 建议将价格相关引入 `currency`、`billing_period`（month/year）
  * 约束：`benefits (plan_id, feature_id)` 唯一索引，避免重复
* **system_configs 扩展**

  * 增：`category`(`prompt|copy|system|experiment`)、`page`、`locale`、`version INT`、`is_published BOOL`
  * 对热 key 增加**乐观锁**字段 `etag`/`version`
* **feature_definitions**

  * 增：`form_schema_id`、`pipeline_schema_id`、`status`、`visibility`（free/vip/enterprise）
  * 为“向导发布”留出审计：`published_by`、`published_at`

## 8) 数据迁移（硬编码 → 数据库）

* 编写幂等迁移脚本：扫描现有硬编码 provider 映射/默认表单/默认流程 → 生成插入 SQL/Knex seeds
* 对每条迁移生成**回滚快照**（JSON）写入 `config_snapshots`（新表）：`{type, refId, before, after, actor, ts}`

## 9) 缓存与“更新即生效”

* **缓存哪些**：`system_configs` 热 key、`feature_definitions`、`form_schemas`（最新发布版）、`pipeline_schemas`、`provider_endpoints`

* **Key 约定**：`cfg:feature:{id}`, `cfg:form:{id}:v:{ver}`, `cfg:pipeline:{id}:v:{ver}`, `cfg:provider:{id}`, `cfg:copy:{page}:{locale}`

* **TTL**：大多数配置 **不过期**（永久+版本号），依赖 **显式失效**；列表页可设置 60~300s TTL

* **一致性**：

  * 后端管理接口成功写 DB 后 → **发布 Redis Pub/Sub** `channel=config.changed` ，载荷 `{type,id,version}`
  * 所有 PM2 进程订阅 → 清本地 LRU/Redis Key → 拉取最新
  * 前端通过 SWR/React Query 监听 SSE/轮询 `ETag`，自动刷新

* **降级**：

  * 维护 `last_good/` 本地 JSON 快照（写入时覆盖），DB/Redis 故障时读取快照继续服务（只读）
  * 关键路径配置设 **内建默认值**（编译期 JSON）

---

# 三、工作量拆解与时间表

## 10) 任务粒度（4–12 小时/项）与并行性

* **必须串行**：Provider 动态加载底座 → 现有 Provider 适配；Pipeline 校验 → 测试运行器；缓存广播底座 → 前端自动刷新
* **可并行**：表单设计器 UI 与 字段库实现；Prompt 中心与内容配置；E2E 测试与性能基线

## 11) 按周实施计划（建议）

* **Week 1（MVP 地基）**

  * CMS-001/002/003/004/005/009/012/014/017/020/021/033/039（详见任务卡）
  * 交付：Provider 动态加载闭环；表单 Schema 生/读/写（基础 UI）；React Flow 画布 + 基本节点 + DAG 校验；缓存广播与迁移脚本

* **Week 2（MVP 完成）**

  * CMS-010/011/013/015/018/019/022/029/030/037/040/034
  * 交付：10 类字段 + 配置面板 + 实时预览；流程编辑器侧边栏与并行/条件/Join；测试运行器（前后端）；公告/轮播最小集；E2E 关键流；降级回退闭环；前端自动刷新

* **Week 3（增强与稳定）**

  * CMS-025/026/027/028/031/032/035/036/038
  * 交付：Prompt 中心（Monaco + Handlebars + 版本/测试）；会员套餐/权益；文案多语言；RBAC；审计回滚；性能与健康

* **Week 4（可选，P1/P2 打磨）**

  * CMS-006/007/016/023/024 及优化项
  * 交付：Provider 健康监控、可视化执行、虚拟滚动、执行日志/进度优化

## 12) MVP 与迭代策略

* **MVP（2 周）**：CMS-001~005, 009~015, 017~022, 029~030, 033~034, 037, 039~040

* **P1（尽快）**：CMS-006, 016, 023~028, 031~036, 038

* **P2（可后置）**：CMS-007, 024（执行可视化深度）、国际化扩展、健康看板

* **主系统降级策略**：

  * CMS 故障 → 走本地快照/默认内置配置；前端隐藏新增功能入口，保留已发布功能；后台管理只读提示
  * Pipeline 执行失败 → 自动重试一次 + 配额返还（你已有）+ 记录可追踪日志

---

# 四、风险识别与应对

* **React Flow 坑点**：大图渲染卡顿 → lazy mount 节点、禁用 shadow/过度动画、使用 `useMemo` 缓存布局；边线过多时启用简化路由
* **Form 设计器**：受控组件频繁渲染 → react-hook-form 控制粒度 + `Controller`；图片字段大文件预览注意内存释放
* **Provider 动态加载**：安全风险（任意 require）→ handler 白名单 + 构建期注册表；禁用 eval/child_process；凭证加密与轮换
* **配置更新实时生效**：PM2 多进程缓存一致性 → Redis Pub/Sub；失败重试与幂等
* **单人开发**：卡点风险 → 任务颗粒度 4–8h；先通路后打磨；遇阻先以“可运行的最小版本”保进度；善用 Claude Code/Cursor 的“从报错出发修复”模式
* **业务风险**：CMS 变更误发布 → 发布确认+回滚快照；灰度开关（只对管理员/内测用户可见）

---

# 五、AI 编程任务卡（40 项）

> 说明：以下任务卡已按模块与周次分配。**依赖以 taskId 标识**；可直接拷贝给 Claude Code/Cursor/GPT-5 Pro 使用。每条 `aiPromptSuggestion` 皆为**可执行指令**风格。

---

## A. Provider 管理（8）

```json
{
  "taskId": "CMS-001",
  "module": "Provider管理",
  "title": "实现Provider动态加载机制",
  "phase": "Week 1",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "重构 pipeline 引擎 Provider 获取逻辑为动态加载，支持缓存与错误包装。",
  "technicalRequirements": [
    "在 provider_endpoints 增加 handler_key/auth/status/last_called_at/deleted_at",
    "实现 ProviderLoader：白名单目录扫描或静态注册表 + 实例缓存",
    "凭证字段 AES-256-CBC 加密存储，运行时解密",
    "仅允许 handler_key 映射已注册的 Provider 类，禁止任意路径"
  ],
  "acceptanceCriteria": [
    "可在 DB 新增一个 MockProvider 配置并被动态调用",
    "动态加载开销 < 5ms（缓存命中时）",
    "异常时返回标准错误结构并记录日志"
  ],
  "codeLocations": [
    "backend/src/services/pipelineEngine.service.js",
    "backend/src/providers/ProviderLoader.ts",
    "backend/src/providers/impl/*"
  ],
  "testStrategy": [
    "单测 ProviderLoader.load() 成功/失败/缓存命中",
    "集成测：pipeline 执行使用动态 Provider"
  ],
  "aiPromptSuggestion": "请在后端实现 ProviderLoader（TypeScript），读取 provider_endpoints 的 handler_key，依据白名单注册表返回对应 Provider 实例；实现实例缓存；异常时抛出标准业务错误。修改 pipelineEngine.service.js 的 getProvider 为调用 ProviderLoader。"
}
```

```json
{
  "taskId": "CMS-002",
  "module": "Provider管理",
  "title": "定义 IProvider 接口与 BaseProvider 基类",
  "phase": "Week 1",
  "estimatedHours": 4,
  "priority": "P0",
  "dependencies": ["CMS-001"],
  "description": "统一 Provider 能力与生命周期钩子。",
  "technicalRequirements": [
    "IProvider: validate(params), execute(params, ctx), health()?",
    "BaseProvider: 通用重试/超时/headers 组装、日志埋点"
  ],
  "acceptanceCriteria": [
    "现有 Provider 可编译通过并继承基类",
    "BaseProvider 支持可配置超时/重试策略"
  ],
  "codeLocations": [
    "backend/src/providers/IProvider.ts",
    "backend/src/providers/base/BaseProvider.ts"
  ],
  "testStrategy": ["单测接口签名与基类重试逻辑"],
  "aiPromptSuggestion": "创建 IProvider 接口和 BaseProvider 抽象类，内置超时与重试；调整现有 Provider 以继承该基类。"
}
```

```json
{
  "taskId": "CMS-003",
  "module": "Provider管理",
  "title": "重构现有 Provider 适配 IProvider",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-001","CMS-002"],
  "description": "将 TencentCI/RunningHub/SCF Provider 改造为实现 IProvider。",
  "technicalRequirements": [
    "保留现有业务参数",
    "统一错误码与日志格式"
  ],
  "acceptanceCriteria": [
    "三种 Provider 全部通过集成测试",
    "错误可观测：含 provider, endpointId, requestId"
  ],
  "codeLocations": ["backend/src/providers/impl/*"],
  "testStrategy": ["用 mock server 断言超时/重试/错误路径"],
  "aiPromptSuggestion": "按 IProvider 规范重构 TencentCI、RunningHub、SCF Provider；保留原有参数与行为；补齐错误包装。"
}
```

```json
{
  "taskId": "CMS-004",
  "module": "Provider管理",
  "title": "Provider CRUD 后端接口",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-001"],
  "description": "实现 provider_endpoints 的增删改查、软删除、连通性测试接口（后端）。",
  "technicalRequirements": [
    "POST/PUT/GET/LIST/DELETE(soft) REST",
    "连通性测试：以 provider 的 health 或一次轻量请求为准",
    "写入成功后发布 Redis Pub/Sub 失效通知"
  ],
  "acceptanceCriteria": [
    "OpenAPI 文档可用",
    "写 DB 成功会广播 config.changed 事件"
  ],
  "codeLocations": ["backend/src/routes/providers.ts","backend/src/services/providers.service.ts"],
  "testStrategy": ["接口单测 + 连通性模拟测"],
  "aiPromptSuggestion": "实现 Provider 后端 CRUD 与测试连接接口，写入成功后通过 Redis 发布 config.changed。"
}
```

```json
{
  "taskId": "CMS-005",
  "module": "Provider管理",
  "title": "Provider 管理前端页面",
  "phase": "Week 1",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-004"],
  "description": "Provider 列表/创建/编辑/禁用/测试连接的前端页面（AntD）。",
  "technicalRequirements": [
    "表单含：名称/类型/handler_key/BaseURL/认证方式/超时/重试",
    "创建/编辑时加密凭证提示，不回传明文",
    "操作完成后前端缓存刷新"
  ],
  "acceptanceCriteria": [
    "能创建并测试一个新 Provider",
    "禁用后不可被流程选择"
  ],
  "codeLocations": ["frontend/app/admin/providers/*"],
  "testStrategy": ["Playwright 覆盖新增/编辑/测试连接流程"],
  "aiPromptSuggestion": "用 AntD 构建 Provider 管理 UI，串联后端接口，完成新增/编辑/禁用/测试连接。"
}
```

```json
{
  "taskId": "CMS-006",
  "module": "Provider管理",
  "title": "Provider 测试连接（前端交互与结果持久）",
  "phase": "Week 4",
  "estimatedHours": 4,
  "priority": "P1",
  "dependencies": ["CMS-004","CMS-005"],
  "description": "增强测试结果展示与最近一次结果写回 DB。",
  "technicalRequirements": ["记录 last_called_at/status","Toast + 详情抽屉展示"],
  "acceptanceCriteria": ["列表可见上次测试状态与时间"],
  "codeLocations": ["frontend/app/admin/providers/*","backend/src/services/providers.service.ts"],
  "testStrategy": ["前端交互测试"],
  "aiPromptSuggestion": "完善测试连接流程，将结果写回 DB 并在列表展示。"
}
```

```json
{
  "taskId": "CMS-007",
  "module": "Provider管理",
  "title": "Provider 健康监控聚合",
  "phase": "Week 4",
  "estimatedHours": 8,
  "priority": "P2",
  "dependencies": ["CMS-003","CMS-004"],
  "description": "聚合成功率/平均响应时间，简单趋势图。",
  "technicalRequirements": ["存储近 24h 聚合指标","开放只读接口"],
  "acceptanceCriteria": ["前端可见健康卡片与趋势"],
  "codeLocations": ["backend/src/metrics/*","frontend/app/admin/providers/health/*"],
  "testStrategy": ["模拟数据回放"],
  "aiPromptSuggestion": "实现 Provider 健康指标聚合与前端可视化。"
}
```

```json
{
  "taskId": "CMS-008",
  "module": "Provider管理",
  "title": "凭证加密与密钥轮换",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-004"],
  "description": "新增/更新 Provider 时加密敏感字段，并支持主密钥轮换。",
  "technicalRequirements": [
    "AES-256-CBC，密钥来自环境变量",
    "提供轮换脚本：K1→K2 双读切换期"
  ],
  "acceptanceCriteria": ["密钥切换后不影响调用"],
  "codeLocations": ["backend/src/utils/crypto.ts","scripts/rotate-key.ts"],
  "testStrategy": ["密钥轮换回归测"],
  "aiPromptSuggestion": "实现 AES 加密工具与密钥轮换脚本，并接入 Provider 写入流程。"
}
```

---

## B. 表单设计器（8）

```json
{
  "taskId": "CMS-009",
  "module": "表单设计器",
  "title": "设计器基架搭建（react-hook-form + zod + dnd-kit）",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": [],
  "description": "创建设计器容器、字段面板、画布与属性侧栏的基本骨架。",
  "technicalRequirements": ["dnd-kit 拖拽","zodResolver","AntD 布局"],
  "acceptanceCriteria": ["可拖入占位字段并保存基本 schema"],
  "codeLocations": ["frontend/app/admin/forms/designer/*"],
  "testStrategy": ["UI 基础交互测"],
  "aiPromptSuggestion": "搭建表单设计器框架（dnd + 属性侧栏），集成 react-hook-form 与 zod。"
}
```

```json
{
  "taskId": "CMS-010",
  "module": "表单设计器",
  "title": "实现10类字段渲染与编辑",
  "phase": "Week 2",
  "estimatedHours": 12,
  "priority": "P0",
  "dependencies": ["CMS-009"],
  "description": "支持 image/text/number/select/radio/checkbox/slider/color/date/textarea",
  "technicalRequirements": ["字段组件拆分与通用 props 约定","必填/默认值/占位/帮助"],
  "acceptanceCriteria": ["10 类字段均可拖入、配置、渲染"],
  "codeLocations": ["frontend/app/admin/forms/designer/fields/*"],
  "testStrategy": ["Playwright 覆盖添加/编辑/删除字段"],
  "aiPromptSuggestion": "为 10 种字段分别实现渲染与配置组件，统一受控接口。"
}
```

```json
{
  "taskId": "CMS-011",
  "module": "表单设计器",
  "title": "字段配置面板与校验规则编辑",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-010"],
  "description": "在侧栏编辑标签/占位/验证（required/min/max/regex）等。",
  "technicalRequirements": ["规则转 zod schema","表单级错误提示"],
  "acceptanceCriteria": ["错误提示清晰，导出 schema 含校验规则"],
  "codeLocations": ["frontend/app/admin/forms/designer/panels/*"],
  "testStrategy": ["规则边界用例测"],
  "aiPromptSuggestion": "实现字段属性侧栏，编辑并导出对应 zod 规则。"
}
```

```json
{
  "taskId": "CMS-012",
  "module": "表单设计器",
  "title": "FormSchema 生成器与解析器（后端持久化）",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": [],
  "description": "定义 schema 结构与版本，提供 CRUD API 与发布标记。",
  "technicalRequirements": ["versioning + draft/published","OpenAPI"],
  "acceptanceCriteria": ["可创建/更新/发布/回滚一个表单"],
  "codeLocations": ["backend/src/routes/forms.ts","backend/src/services/forms.service.ts"],
  "testStrategy": ["接口单测 + 回滚用例"],
  "aiPromptSuggestion": "实现 form_schemas 的 CRUD/发布/回滚接口与服务层。"
}
```

```json
{
  "taskId": "CMS-013",
  "module": "表单设计器",
  "title": "实时预览（同 Schema 渲染）",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-010","CMS-012"],
  "description": "右侧预览窗根据当前 schema 实时渲染，支持默认值与校验反馈。",
  "technicalRequirements": ["避免全量重渲染","隔离设计态与预览态状态"],
  "acceptanceCriteria": ["预览与设计所见即所得，不卡顿"],
  "codeLocations": ["frontend/app/admin/forms/preview/*"],
  "testStrategy": ["渲染性能测试"],
  "aiPromptSuggestion": "实现基于当前 schema 的实时预览区域并接入校验反馈。"
}
```

```json
{
  "taskId": "CMS-014",
  "module": "表单设计器",
  "title": "表单 Schema 持久化 API（前端接入）",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-012"],
  "description": "前端设计器接入后端 CRUD/发布接口，支持草稿自动保存。",
  "technicalRequirements": ["节流保存","发布前校验"],
  "acceptanceCriteria": ["草稿自动保存与恢复可靠"],
  "codeLocations": ["frontend/app/admin/forms/designer/*"],
  "testStrategy": ["断网/刷新恢复用例"],
  "aiPromptSuggestion": "为设计器接入后端接口，完成草稿保存与发布流程。"
}
```

```json
{
  "taskId": "CMS-015",
  "module": "表单设计器",
  "title": "拖拽/排序/复制增强与 UX 打磨",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P1",
  "dependencies": ["CMS-009","CMS-010"],
  "description": "支持字段复制、组移动、快捷键删除、撤销/重做（基础）。",
  "technicalRequirements": ["操作历史栈","可访问性"],
  "acceptanceCriteria": ["复杂表单编辑流畅"],
  "codeLocations": ["frontend/app/admin/forms/designer/*"],
  "testStrategy": ["交互回归测"],
  "aiPromptSuggestion": "增强拖拽与历史栈，提升设计体验。"
}
```

```json
{
  "taskId": "CMS-016",
  "module": "表单设计器",
  "title": "大表单性能优化（虚拟滚动/分组折叠）",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-013","CMS-015"],
  "description": "超 50 字段时启用虚拟滚动与分组折叠。",
  "technicalRequirements": ["滚动容器与尺寸测量","避免多余 re-render"],
  "acceptanceCriteria": [">100 字段仍保持流畅（>50fps）"],
  "codeLocations": ["frontend/app/admin/forms/designer/*"],
  "testStrategy": ["性能基准脚本"],
  "aiPromptSuggestion": "为大表单启用虚拟列表与分组折叠，确保流畅。"
}
```

---

## C. 流程编辑器（8）

```json
{
  "taskId": "CMS-017",
  "module": "流程编辑器",
  "title": "React Flow 基础集成与自定义节点框架",
  "phase": "Week 1",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "引入 React Flow、定义节点/边基础类型与渲染、吸附与缩放。",
  "technicalRequirements": ["画布性能考虑","自定义节点入口/出口锚点"],
  "acceptanceCriteria": ["可创建/连接基础节点"],
  "codeLocations": ["frontend/app/admin/pipelines/designer/*"],
  "testStrategy": ["画布交互用例"],
  "aiPromptSuggestion": "集成 React Flow 并实现自定义节点渲染框架与连线。"
}
```

```json
{
  "taskId": "CMS-018",
  "module": "流程编辑器",
  "title": "实现四类节点(API/SCF/CONDITION/PARALLEL) + JOIN/END",
  "phase": "Week 2",
  "estimatedHours": 10,
  "priority": "P0",
  "dependencies": ["CMS-017"],
  "description": "完成核心节点组件与默认参数面板骨架。",
  "technicalRequirements": ["节点图标/颜色规范","节点校验钩子"],
  "acceptanceCriteria": ["可拖入并连线，属性可编辑"],
  "codeLocations": ["frontend/app/admin/pipelines/designer/nodes/*"],
  "testStrategy": ["节点连线规则测试"],
  "aiPromptSuggestion": "实现 6 类节点的可视化组件与基础配置。"
}
```

```json
{
  "taskId": "CMS-019",
  "module": "流程编辑器",
  "title": "节点配置侧边栏（Drawer）",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-018"],
  "description": "选中节点弹出侧栏编辑：Provider 选择、参数、重试、超时。",
  "technicalRequirements": ["与 Provider 列表联动","校验提示"],
  "acceptanceCriteria": ["侧栏编辑即刻反映到节点数据"],
  "codeLocations": ["frontend/app/admin/pipelines/designer/panels/*"],
  "testStrategy": ["交互与校验用例"],
  "aiPromptSuggestion": "实现节点配置侧栏并与 Provider/表单变量联动。"
}
```

```json
{
  "taskId": "CMS-020",
  "module": "流程编辑器",
  "title": "Pipeline Schema 序列化/反序列化与版本管理",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-017"],
  "description": "将画布结构转为 JSON Schema 并持久化（CRUD/发布/回滚）。",
  "technicalRequirements": ["versioning + draft/published","OpenAPI"],
  "acceptanceCriteria": ["可创建/更新/发布/回滚一个流程"],
  "codeLocations": ["backend/src/routes/pipelines.ts","backend/src/services/pipelines.service.ts"],
  "testStrategy": ["接口单测"],
  "aiPromptSuggestion": "实现 pipeline_schemas 的持久化接口与版本控制。"
}
```

```json
{
  "taskId": "CMS-021",
  "module": "流程编辑器",
  "title": "合法性验证算法（DAG/规则/依赖）",
  "phase": "Week 1",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-020"],
  "description": "实现无环检查、入/出度规则、参数来源与并行汇聚校验。",
  "technicalRequirements": ["拓扑排序/DFS","规则可扩展"],
  "acceptanceCriteria": ["非法流程给出明确报错，合法流程通过"],
  "codeLocations": ["backend/src/services/pipeline/validator.ts"],
  "testStrategy": ["覆盖典型非法拓扑与参数缺失用例"],
  "aiPromptSuggestion": "编写 Pipeline 校验器，输出可读错误清单。"
}
```

```json
{
  "taskId": "CMS-022",
  "module": "流程编辑器",
  "title": "测试运行器（后端执行接口）",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-021","CMS-003"],
  "description": "接收表单示例数据 + 流程 JSON，调用现有引擎试跑，返回节点级日志与结果。",
  "technicalRequirements": ["注入模拟上下文","安全限流"],
  "acceptanceCriteria": ["可在后台一键试跑并观察日志"],
  "codeLocations": ["backend/src/routes/pipelines.testRun.ts","backend/src/services/pipeline/testRunner.ts"],
  "testStrategy": ["集成测：试跑成功/失败/重试路径"],
  "aiPromptSuggestion": "实现 /pipelines/test-run 接口，串联现有引擎输出节点级日志。"
}
```

```json
{
  "taskId": "CMS-023",
  "module": "流程编辑器",
  "title": "测试运行器前端 UI",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-022"],
  "description": "在发布前提供“输入示例 → 运行 → 日志/耗时/结果”视图。",
  "technicalRequirements": ["日志按节点折叠","失败高亮"],
  "acceptanceCriteria": ["可视化一键试跑闭环完整"],
  "codeLocations": ["frontend/app/admin/pipelines/test-run/*"],
  "testStrategy": ["UI 回归测"],
  "aiPromptSuggestion": "构建试跑 UI，展示结果与日志并支持再次运行。"
}
```

```json
{
  "taskId": "CMS-024",
  "module": "流程编辑器",
  "title": "执行可视化（进度/甘特/节点耗时）",
  "phase": "Week 4",
  "estimatedHours": 8,
  "priority": "P2",
  "dependencies": ["CMS-022"],
  "description": "提供执行时间线与节点耗时分布，便于排障与优化。",
  "technicalRequirements": ["简易甘特/时间线","下载日志"],
  "acceptanceCriteria": ["能定位慢节点并导出日志"],
  "codeLocations": ["frontend/app/admin/pipelines/insights/*"],
  "testStrategy": ["性能数据回放"],
  "aiPromptSuggestion": "新增执行可视化页面，展示耗时与关键日志并支持导出。"
}
```

---

## D. Prompt 管理中心（4）

```json
{
  "taskId": "CMS-025",
  "module": "Prompt管理",
  "title": "集成 Monaco Editor + 变量提示",
  "phase": "Week 3",
  "estimatedHours": 4,
  "priority": "P0",
  "dependencies": [],
  "description": "嵌入 Monaco，配置 {{var}} 高亮与补全。",
  "technicalRequirements": ["变量清单来自 schema.variables","键入时提示"],
  "acceptanceCriteria": ["编辑体验与高亮正常"],
  "codeLocations": ["frontend/app/admin/prompts/editor/*"],
  "testStrategy": ["交互测试"],
  "aiPromptSuggestion": "集成 Monaco，支持 {{变量}} 高亮与自动补全。"
}
```

```json
{
  "taskId": "CMS-026",
  "module": "Prompt管理",
  "title": "Handlebars 渲染与实时预览",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-025"],
  "description": "实现变量替换与右侧实时预览；错误提示定位。",
  "technicalRequirements": ["#if/#each 支持","语法错误高亮"],
  "acceptanceCriteria": ["输入示例变量可实时渲染"],
  "codeLocations": ["frontend/app/admin/prompts/preview/*","backend/src/services/prompt/renderer.ts"],
  "testStrategy": ["变量边界/转义用例"],
  "aiPromptSuggestion": "实现 Handlebars 渲染与预览，处理语法错误与转义。"
}
```

```json
{
  "taskId": "CMS-027",
  "module": "Prompt管理",
  "title": "Prompt CRUD + 版本表",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-026"],
  "description": "后端存储 Prompt、变量清单、描述；保存版本历史并支持回滚。",
  "technicalRequirements": ["prompt_versions 表","回滚写快照"],
  "acceptanceCriteria": ["可查看历史与一键回滚"],
  "codeLocations": ["backend/src/routes/prompts.ts","backend/src/services/prompt.service.ts"],
  "testStrategy": ["版本/回滚用例"],
  "aiPromptSuggestion": "实现 Prompt 的版本化存储与回滚接口，并接入前端。"
}
```

```json
{
  "taskId": "CMS-028",
  "module": "Prompt管理",
  "title": "Prompt 在线测试调用工具",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-026","CMS-003"],
  "description": "渲染模板后调用选定 Provider/Workflow，展示响应（带风控）。",
  "technicalRequirements": ["速率限制","敏感词过滤/审计"],
  "acceptanceCriteria": ["能对比不同版本的测试结果"],
  "codeLocations": ["frontend/app/admin/prompts/tester/*","backend/src/routes/prompts.test.ts"],
  "testStrategy": ["限流/错误模拟"],
  "aiPromptSuggestion": "实现 Prompt 在线测试面板，支持选择 Provider 并查看响应。"
}
```

---

## E. 内容配置（4）

```json
{
  "taskId": "CMS-029",
  "module": "内容管理",
  "title": "公告模块（后端+前端最小集）",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-033"],
  "description": "公告 CRUD、定时发布、目标用户、前端展示。",
  "technicalRequirements": ["发布时广播 config.changed","草稿/发布/过期状态机"],
  "acceptanceCriteria": ["按时间与目标受众正确展示"],
  "codeLocations": ["backend/src/routes/announcements.ts","frontend/app/admin/announcements/*"],
  "testStrategy": ["时间/受众过滤用例"],
  "aiPromptSuggestion": "实现公告的增删改查与发布、到期逻辑，并在前端渲染。"
}
```

```json
{
  "taskId": "CMS-030",
  "module": "内容管理",
  "title": "轮播图模块（COS 上传 + 排序）",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "轮播图 CRUD、COS 上传、拖拽排序、定时上下线。",
  "technicalRequirements": ["存 image_key 和 image_url","排序写回"],
  "acceptanceCriteria": ["前端按排序与状态渲染"],
  "codeLocations": ["backend/src/routes/banners.ts","frontend/app/admin/banners/*"],
  "testStrategy": ["上传/排序/上下线用例"],
  "aiPromptSuggestion": "实现轮播图管理，接入 COS 上传与拖拽排序。"
}
```

```json
{
  "taskId": "CMS-031",
  "module": "内容管理",
  "title": "会员套餐与权益（plans + benefits）",
  "phase": "Week 3",
  "estimatedHours": 10,
  "priority": "P1",
  "dependencies": [],
  "description": "套餐 CRUD、价格与配额、与 feature 关联（勾选）。",
  "technicalRequirements": ["唯一索引 (plan_id,feature_id)","排序与推荐位"],
  "acceptanceCriteria": ["前端可选择展示套餐并映射权限"],
  "codeLocations": ["backend/src/routes/memberships.ts","frontend/app/admin/memberships/*"],
  "testStrategy": ["关联/去关联用例"],
  "aiPromptSuggestion": "实现会员套餐与权益管理，前端选择 feature 并保存。"
}
```

```json
{
  "taskId": "CMS-032",
  "module": "内容管理",
  "title": "文案配置与多语言（最小集）",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P1",
  "dependencies": ["CMS-033"],
  "description": "按 page/locale 管理 key-value 文案，前端拉取展示。",
  "technicalRequirements": ["system_configs 分类 copy","ETag/版本号"],
  "acceptanceCriteria": ["切换 locale 文案即时生效"],
  "codeLocations": ["backend/src/routes/copy.ts","frontend/app/admin/copy/*"],
  "testStrategy": ["缓存刷新用例"],
  "aiPromptSuggestion": "实现页面文案管理与国际化最小闭环。"
}
```

---

## F. 平台与运维（4）

```json
{
  "taskId": "CMS-033",
  "module": "平台与运维",
  "title": "配置缓存层（Redis）与 Pub/Sub 广播",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": [],
  "description": "封装 ConfigService，含 get/set、版本/ETag、发布 config.changed。",
  "technicalRequirements": ["Key 规范","本地 LRU 缓存"],
  "acceptanceCriteria": ["更新配置后 1s 内各进程生效"],
  "codeLocations": ["backend/src/services/config.service.ts"],
  "testStrategy": ["多进程一致性用例"],
  "aiPromptSuggestion": "实现 ConfigService：Redis 缓存 + Pub/Sub 广播 + 本地 LRU。"
}
```

```json
{
  "taskId": "CMS-034",
  "module": "平台与运维",
  "title": "前端 SWR/ETag 自动刷新机制",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-033"],
  "description": "当后端配置版本变更时，前端自动重新获取。",
  "technicalRequirements": ["SWR 轮询或 SSE","ETag 对比"],
  "acceptanceCriteria": ["发布后前端 5s 内刷新展示"],
  "codeLocations": ["frontend/lib/configClient.ts","frontend/app/*"],
  "testStrategy": ["ETag 变化触发刷新用例"],
  "aiPromptSuggestion": "实现基于 SWR 的 ETag 检测与自动刷新。"
}
```

```json
{
  "taskId": "CMS-035",
  "module": "平台与运维",
  "title": "CMS 管理 RBAC（角色与路由守卫）",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P1",
  "dependencies": [],
  "description": "角色：admin/editor/viewer；基于路由/操作的权限控制。",
  "technicalRequirements": ["服务端鉴权中间件","前端路由守卫"],
  "acceptanceCriteria": ["不同角色看到的功能按钮不同"],
  "codeLocations": ["backend/src/middlewares/authz.ts","frontend/app/(admin)/*"],
  "testStrategy": ["权限边界用例"],
  "aiPromptSuggestion": "实现基于角色的权限控制与前端路由守卫。"
}
```

```json
{
  "taskId": "CMS-036",
  "module": "平台与运维",
  "title": "审计日志与回滚快照",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P1",
  "dependencies": ["CMS-012","CMS-020","CMS-027"],
  "description": "对表单/流程/Prompt/文案变更写 config_snapshots，支持回滚。",
  "technicalRequirements": ["before/after JSON","actor/timestamp"],
  "acceptanceCriteria": ["可在 UI 查看并一键回滚"],
  "codeLocations": ["backend/src/services/audit.service.ts","frontend/app/admin/audit/*"],
  "testStrategy": ["回滚幂等性用例"],
  "aiPromptSuggestion": "实现审计快照表与回滚 API，并提供最小 UI。"
}
```

---

## G. 测试与优化（4）

```json
{
  "taskId": "CMS-037",
  "module": "测试与优化",
  "title": "E2E 关键流测试（Playwright）",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-005","CMS-010","CMS-018","CMS-029","CMS-030"],
  "description": "覆盖：创建 Provider → 创建表单 → 创建流程 → 发布 → 运行测试。",
  "technicalRequirements": ["CI 脚本与截图保存"],
  "acceptanceCriteria": ["关键流 100% 通过"],
  "codeLocations": ["e2e/*.spec.ts"],
  "testStrategy": ["CI 里跑通"],
  "aiPromptSuggestion": "编写 Playwright 用例覆盖 CMS 关键路径并接入 CI。"
}
```

```json
{
  "taskId": "CMS-038",
  "module": "测试与优化",
  "title": "性能与稳定性基准（后端/前端）",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-022","CMS-034"],
  "description": "后端接口 P95 延迟、前端画布交互 FPS、内存泄露检查。",
  "technicalRequirements": ["k6/Autocannon + Lighthouse"],
  "acceptanceCriteria": ["后端 P95 < 150ms，画布交互 > 50fps"],
  "codeLocations": ["scripts/bench/*"],
  "testStrategy": ["固定数据集压测"],
  "aiPromptSuggestion": "提供后端/前端性能基线脚本并输出报告。"
}
```

```json
{
  "taskId": "CMS-039",
  "module": "测试与优化",
  "title": "迁移脚本（硬编码 → DB/快照）",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-012","CMS-020","CMS-033"],
  "description": "将现有硬编码的 Provider/表单/流程迁移到 DB，并写快照。",
  "technicalRequirements": ["幂等执行","失败回滚"],
  "acceptanceCriteria": ["迁移后系统可正常运行"],
  "codeLocations": ["scripts/migrate/*"],
  "testStrategy": ["预生产演练"],
  "aiPromptSuggestion": "编写迁移脚本，将硬编码配置批量导入 DB 并生成回滚快照。"
}
```

```json
{
  "taskId": "CMS-040",
  "module": "测试与优化",
  "title": "降级策略与本地快照回退",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-033","CMS-036"],
  "description": "DB/Redis 故障时读取 last_good 本地 JSON 快照，提供只读模式。",
  "technicalRequirements": ["写入快照在发布成功后","只读提示与监控报警"],
  "acceptanceCriteria": ["断 Redis/DB 场景下主流程可运行"],
  "codeLocations": ["backend/src/services/fallback.service.ts"],
  "testStrategy": ["容灾演练脚本"],
  "aiPromptSuggestion": "实现本地快照回退逻辑及只读降级提示，编写故障演练脚本。"
}
```

---

# 六、最佳实践建议（要点）

* **统一错误模型**（code/message/context/requestId），便于链路追踪
* **日志分层**：应用日志（info/error）+ 审计日志（变更）+ 指标日志（执行耗时）
* **接口幂等**：发布/回滚/迁移类操作必须带 `version/etag` 校验
* **灰度与可见性**：Feature 的 `visibility` 与“内测名单”结合，支持小流量发布
* **图形编辑性能**：React Flow 大图禁用阴影/渐变；节点内容尽量轻；边线使用直线优先
* **安全**：Provider 白名单机制 + 只读文件系统假设；所有外部请求设置超时/重试与最大响应体限制

---

## 你可以立即开工的方法

1. 直接从 **Week 1** 的 P0 任务开始（CMS-001/002/003/004/005/012/017/020/021/033/039）。
2. 将每个任务卡丢给 Claude Code/Cursor/GPT-5 Pro，一次一张，完成即提交 PR。
3. 每日收尾运行 **CMS-037** 的 E2E 关键流，确保没破坏主链路。
4. 任一关键任务遇阻，先产出“能跑的最小版本”，把风险后移到 Week 3/4 细化。

需要我把这些任务卡导出为 **JSON 文件**或按 Trello/Jira 导入格式生成吗？我也可以再为每张卡补充**OpenAPI 小节骨架**与**数据库迁移 SQL 模版**（不写长代码，仅给骨架），你直接交给 AI 编程工具即可。



回答二：
基于您的技术背景和需求，我将作为资深架构师提供详细的CMS自研实施规划。

# CMS配置系统实施规划

## 第一部分：技术架构设计

### 1. 表单设计器技术方案

**推荐方案：混合方案（Form.io核心 + 自研UI）**

```typescript
// JSON Schema设计示例
{
  "title": "AI模特生成",
  "description": "生成虚拟模特试穿效果",
  "fields": [
    {
      "key": "clothingImage",
      "type": "image",
      "label": "服装图片",
      "placeholder": "请上传服装图片",
      "required": true,
      "validation": {
        "maxSize": 5242880,
        "allowedFormats": ["jpg", "png", "webp"]
      },
      "ui": {
        "component": "ImageUploader",
        "preview": true,
        "multiple": false
      }
    },
    {
      "key": "modelStyle",
      "type": "select",
      "label": "模特风格",
      "options": [
        {"label": "欧美", "value": "western"},
        {"label": "亚洲", "value": "asian"},
        {"label": "非洲", "value": "african"}
      ],
      "default": "asian"
    },
    {
      "key": "background",
      "type": "radio",
      "label": "背景设置",
      "options": [
        {"label": "纯色", "value": "solid"},
        {"label": "室内", "value": "indoor"},
        {"label": "室外", "value": "outdoor"}
      ],
      "default": "solid"
    },
    {
      "key": "quality",
      "type": "slider",
      "label": "生成质量",
      "min": 1,
      "max": 10,
      "step": 1,
      "default": 7
    },
    {
      "key": "advancedOptions",
      "type": "object",
      "label": "高级选项",
      "fields": [
        {
          "key": "lighting",
          "type": "select",
          "label": "光照条件",
          "options": ["自然光", "柔光", "强光"]
        },
        {
          "key": "pose",
          "type": "select", 
          "label": "姿势",
          "options": ["站立", "坐姿", "行走"]
        }
      ]
    }
  ]
}
```

**技术优势分析：**
- **Form.io核心引擎**：成熟的表单渲染和验证，避免重复造轮子
- **自研设计器UI**：完全控制用户体验，与现有设计系统一致
- **扩展性**：易于添加自定义字段类型和验证规则

### 2. Pipeline流程编辑器技术方案

**React Flow集成架构：**

```typescript
// Pipeline JSON Schema
interface PipelineSchema {
  id: string;
  name: string;
  version: string;
  steps: PipelineStep[];
  variables: PipelineVariable[];
  errorHandling: ErrorHandlingConfig;
}

interface PipelineStep {
  id: string;
  type: 'api' | 'function' | 'condition' | 'parallel';
  name: string;
  provider?: string;
  config: StepConfig;
  timeout: number;
  retry: RetryPolicy;
  nextSteps: string[]; // 下一步骤ID
  errorSteps?: string[]; // 错误处理步骤
}

// 节点合法性验证算法
class PipelineValidator {
  validate(pipeline: PipelineSchema): ValidationResult {
    // 1. 循环依赖检测
    const cycles = this.detectCycles(pipeline);
    // 2. 步骤依赖分析
    const dependencies = this.analyzeDependencies(pipeline);
    // 3. 变量引用验证
    const variableRefs = this.validateVariableReferences(pipeline);
    
    return { cycles, dependencies, variableRefs, isValid: !cycles.hasCycles };
  }
  
  private detectCycles(pipeline: PipelineSchema): CycleDetectionResult {
    // 使用拓扑排序检测循环
    const graph = this.buildDependencyGraph(pipeline);
    return topologicalSort(graph);
  }
}
```

### 3. Provider动态加载机制

**架构改造方案：**

```typescript
// IProvider接口定义
interface IProvider {
  readonly name: string;
  readonly version: string;
  
  execute(params: any, context: ExecutionContext): Promise<ProviderResult>;
  validate(params: any): ValidationResult;
  testConnection(): Promise<boolean>;
}

// ProviderLoader实现
class ProviderLoader {
  private cache: Map<string, IProvider> = new Map();
  
  async load(providerConfig: ProviderConfig): Promise<IProvider> {
    const cacheKey = `${providerConfig.type}-${providerConfig.handler}`;
    
    if (this.cache.has(cacheKey)) {
      return this.cache.get(cacheKey)!;
    }
    
    // 动态加载Provider类
    const ProviderClass = await this.dynamicImport(providerConfig.handler);
    const provider = new ProviderClass(providerConfig);
    
    // 验证接口实现
    this.validateProviderInterface(provider);
    
    this.cache.set(cacheKey, provider);
    return provider;
  }
  
  private async dynamicImport(handlerPath: string): Promise<any> {
    // 安全限制：只能加载providers目录下的文件
    const safePath = this.validateHandlerPath(handlerPath);
    return require(safePath);
  }
}
```

**安全性措施：**
- 路径白名单验证
- 沙箱环境执行（可选）
- 接口实现验证
- 内存泄漏防护

### 4. Prompt变量替换引擎

**技术选型：Handlebars.js**

```typescript
// Prompt模板引擎
class PromptEngine {
  private handlebars: typeof Handlebars;
  
  constructor() {
    this.handlebars = Handlebars.create();
    this.registerHelpers();
  }
  
  private registerHelpers(): void {
    // 条件渲染
    this.handlebars.registerHelper('if', function(conditional, options) {
      if (conditional) {
        return options.fn(this);
      } else {
        return options.inverse(this);
      }
    });
    
    // 循环渲染
    this.handlebars.registerHelper('each', function(context, options) {
      // 标准each实现
    });
  }
  
  compile(template: string, variables: Record<string, any>): string {
    const compiled = this.handlebars.compile(template);
    return compiled(variables);
  }
  
  extractVariables(template: string): string[] {
    const ast = this.handlebars.parse(template);
    return this.traverseAST(ast);
  }
}
```

### 5. 前端动态表单渲染优化

**技术选型：react-hook-form + 虚拟化**

```typescript
// 大表单性能优化方案
const DynamicForm: React.FC<FormProps> = ({ schema, onSubmit }) => {
  const { control, handleSubmit, watch } = useForm({
    resolver: yupResolver(buildValidationSchema(schema))
  });
  
  // 虚拟滚动容器
  return (
    <VirtualScrollContainer 
      itemCount={schema.fields.length}
      itemHeight={80}
      renderItem={(index) => (
        <FormField 
          field={schema.fields[index]}
          control={control}
          key={schema.fields[index].key}
        />
      )}
    />
  );
};
```

## 第二部分：数据库设计评审

### 6. 数据库表设计改进建议

**改进的announcements表：**
```sql
CREATE TABLE announcements (
  id INT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(255) NOT NULL,
  content TEXT NOT NULL,
  type ENUM('modal', 'banner', 'notification') DEFAULT 'modal',
  priority TINYINT DEFAULT 0, -- 新增：显示优先级
  target_audience JSON, -- 改为JSON，支持复杂条件
  status ENUM('draft', 'published', 'expired', 'archived') DEFAULT 'draft',
  publish_at DATETIME,
  expire_at DATETIME,
  created_by INT, -- 新增：创建人
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  INDEX idx_status_publish (status, publish_at),
  INDEX idx_target_audience ((CAST(target_audience AS CHAR(100)))),
  INDEX idx_priority_status (priority, status)
);
```

**索引策略优化：**
- 复合索引：`(status, publish_at)` 用于查询有效公告
- 函数索引：`target_audience` 的JSON路径查询
- 覆盖索引：高频查询字段

### 7. 配置缓存策略

**Redis缓存设计：**
```typescript
class ConfigCache {
  private redis: Redis;
  private prefix = 'cms:config:';
  
  // 缓存键设计
  private getCacheKey(type: string, id: string | number): string {
    return `${this.prefix}${type}:${id}`;
  }
  
  async getFeatureConfig(featureId: number): Promise<FeatureConfig | null> {
    const key = this.getCacheKey('feature', featureId);
    const cached = await this.redis.get(key);
    
    if (cached) {
      return JSON.parse(cached);
    }
    
    // 缓存未命中，从数据库加载
    const config = await this.loadFromDB(featureId);
    if (config) {
      await this.redis.setex(key, 3600, JSON.stringify(config)); // TTL 1小时
    }
    
    return config;
  }
  
  async updateFeatureConfig(featureId: number, config: FeatureConfig): Promise<void> {
    // 先更新数据库
    await this.saveToDB(featureId, config);
    
    // 再更新缓存
    const key = this.getCacheKey('feature', featureId);
    await this.redis.setex(key, 3600, JSON.stringify(config));
    
    // 发布配置更新事件
    await this.publishConfigUpdate('feature', featureId);
  }
}
```

## 第三部分：工作量拆解和时间表

### 8. 详细工作量拆解（按模块）

**Provider管理模块（P0核心）**
- CMS-001: Provider动态加载机制 (8h)
- CMS-002: IProvider接口定义 (4h) 
- CMS-003: 重构现有Provider (6h)
- CMS-004: Provider管理CRUD接口 (6h)
- CMS-005: Provider管理前端页面 (8h)

**表单设计器模块（P0核心）**
- CMS-006: Form.io集成 (6h)
- CMS-007: 表单设计器UI框架 (12h)
- CMS-008: 字段配置面板 (8h)
- CMS-009: 表单实时预览 (6h)
- CMS-010: JSON Schema生成器 (6h)

**流程编辑器模块（P0核心）**
- CMS-011: React Flow集成 (8h)
- CMS-012: 节点设计器 (10h)
- CMS-013: 连线逻辑实现 (8h)
- CMS-014: Pipeline合法性验证 (6h)
- CMS-015: Pipeline测试运行器 (8h)

### 9. 按周实施计划

**Week 1：基础架构 + Provider管理**
- 目标：完成Provider动态加载，打通配置化基础
- 任务：CMS-001 到 CMS-005
- 交付：Provider管理后台可正常运行

**Week 2：表单设计器核心功能**
- 目标：完成表单设计器MVP
- 任务：CMS-006 到 CMS-010
- 交付：可通过UI设计表单并生成JSON Schema

**Week 3：流程编辑器 + 功能集成**
- 目标：完成流程编辑器和4步向导
- 任务：CMS-011 到 CMS-015
- 交付：完整的"功能卡片创建向导"

**Week 4：内容管理 + 测试优化**
- 目标：完成内容管理模块和系统优化
- 任务：CMS-016 到 CMS-025
- 交付：完整的CMS系统上线

### 10. MVP和迭代策略

**MVP版本（2周交付）：**
- ✅ Provider动态加载
- ✅ 基础表单设计器（支持5种字段）
- ✅ 简单流程编辑器（串行步骤）
- ✅ 功能卡片创建向导（简化版）

**P1功能（后续迭代）：**
- 高级字段类型（对象、数组嵌套）
- 并行执行、条件分支
- Prompt版本历史
- 健康监控面板

## 第四部分：技术风险识别与应对

### 11. 技术风险识别

**Form.io集成风险：**
- 风险：包体积过大，影响前端性能
- 应对：按需引入，Tree Shaking优化

**Provider安全风险：**
- 风险：动态加载恶意代码
- 应对：路径白名单，代码签名验证

**大表单性能风险：**
- 风险：字段过多导致渲染卡顿
- 应对：虚拟滚动，字段懒加载

### 12. 开发风险缓解

**1人开发风险应对：**
- 技术预研：每个模块先做原型验证
- 分阶段交付：每周都有可演示成果
- AI工具辅助：充分利用Claude Code解决技术难题

### 13. 业务风险应对

**降级策略设计：**
```typescript
class ConfigFallback {
  private fallbackConfigs: Map<string, any> = new Map();
  
  async getConfig<T>(key: string, fallbackKey?: string): Promise<T> {
    try {
      // 尝试从CMS获取配置
      return await this.getFromCMS(key);
    } catch (error) {
      // 降级到本地配置
      console.warn(`CMS配置获取失败，使用降级配置: ${key}`);
      return this.getFallbackConfig(fallbackKey || key);
    }
  }
}
```

## 第五部分：AI开发任务卡输出

以下是30个详细的任务卡，可直接交给AI编程工具执行：

### Provider管理模块（6个任务卡）

```json
{
  "taskId": "CMS-001",
  "module": "Provider管理",
  "title": "实现Provider动态加载机制",
  "phase": "Week 1",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "重构pipelineEngine.service.js，将硬编码的Provider映射改为动态加载机制",
  "technicalRequirements": [
    "在provider_endpoints表增加provider_handler字段（存储类名或文件路径）",
    "实现ProviderLoader类，支持动态require加载Provider",
    "所有Provider类实现统一的IProvider接口",
    "添加Provider缓存机制，避免重复加载"
  ],
  "acceptanceCriteria": [
    "新增一个测试Provider（如MockProvider），无需修改pipelineEngine代码即可调用",
    "所有现有Provider（TencentProvider、RunningHubProvider、SCFProvider）正常工作",
    "单元测试覆盖率达到80%",
    "性能测试：动态加载Provider的性能损耗<5ms"
  ],
  "codeLocations": [
    "backend/src/services/pipelineEngine.service.js",
    "backend/src/providers/ProviderLoader.js",
    "backend/src/providers/IProvider.interface.js",
    "backend/src/providers/base/BaseProvider.js"
  ],
  "testStrategy": [
    "单元测试：测试ProviderLoader.load()方法",
    "集成测试：测试Pipeline执行流程，验证动态加载的Provider可正常调用",
    "性能测试：测试动态加载的性能开销"
  ],
  "aiPromptSuggestion": "请帮我重构pipelineEngine.service.js的getProvider方法，实现Provider动态加载。要求：\n1. 从provider_endpoints表读取provider_handler字段（存储Provider类的文件路径）\n2. 使用require()动态加载Provider类\n3. 定义IProvider接口，所有Provider必须实现execute(params)和validate(params)方法\n4. 实现ProviderLoader类，负责Provider的加载和缓存\n5. 添加错误处理：如果Provider加载失败，返回友好错误信息\n6. 编写单元测试，覆盖正常加载、加载失败、缓存机制等场景"
}
```

```json
{
  "taskId": "CMS-002",
  "module": "Provider管理", 
  "title": "定义IProvider接口和BaseProvider基类",
  "phase": "Week 1",
  "estimatedHours": 4,
  "priority": "P0",
  "dependencies": ["CMS-001"],
  "description": "创建标准的IProvider接口和可复用的BaseProvider基类",
  "technicalRequirements": [
    "IProvider接口定义：execute、validate、testConnection方法",
    "BaseProvider抽象类：提供通用功能（请求发送、错误处理、日志记录）",
    "TypeScript接口定义文件",
    "通用错误类型定义"
  ],
  "acceptanceCriteria": [
    "IProvider接口明确定义了所有必需的方法和参数",
    "BaseProvider实现了通用错误处理和日志记录",
    "现有Provider可以轻松继承BaseProvider",
    "TypeScript编译无错误，类型定义完整"
  ],
  "codeLocations": [
    "backend/src/providers/IProvider.interface.ts",
    "backend/src/providers/base/BaseProvider.ts",
    "backend/src/providers/types/ProviderTypes.ts"
  ],
  "testStrategy": [
    "类型测试：验证IProvider接口的类型约束",
    "单元测试：测试BaseProvider的通用方法",
    "集成测试：验证具体Provider正确实现接口"
  ],
  "aiPromptSuggestion": "请帮我创建Node.js的IProvider接口和BaseProvider基类。要求：\n1. 使用TypeScript定义IProvider接口，包含execute(params: any): Promise<any>、validate(params: any): boolean、testConnection(): Promise<boolean>\n2. 创建BaseProvider抽象类，实现通用功能：错误处理、请求日志、超时控制\n3. 定义通用的ProviderResult类型，包含success、data、error、metadata字段\n4. 添加请求重试机制和断路器模式\n5. 编写示例Provider展示如何使用这个基类"
}
```

```json
{
  "taskId": "CMS-003",
  "module": "Provider管理",
  "title": "重构现有Provider实现IProvider接口",
  "phase": "Week 1", 
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-002"],
  "description": "将现有的TencentProvider、RunningHubProvider、SCFProvider重构为符合IProvider接口",
  "technicalRequirements": [
    "每个现有Provider继承BaseProvider",
    "实现IProvider接口的所有方法",
    "保持现有功能完全兼容",
    "添加适当的类型定义"
  ],
  "acceptanceCriteria": [
    "所有现有Provider通过IProvider接口验证",
    "现有Pipeline执行流程不受影响",
    "单元测试覆盖所有Provider方法",
    "类型定义完整，无TypeScript错误"
  ],
  "codeLocations": [
    "backend/src/providers/tencent/TencentProvider.ts",
    "backend/src/providers/runninghub/RunningHubProvider.ts", 
    "backend/src/providers/scf/SCFProvider.ts"
  ],
  "testStrategy": [
    "功能测试：验证每个Provider的核心功能正常",
    "接口测试：验证所有Provider正确实现IProvider接口",
    "回归测试：确保现有业务逻辑不受影响"
  ],
  "aiPromptSuggestion": "请帮我重构现有的Provider类，让它们实现IProvider接口并继承BaseProvider。要求：\n1. 修改TencentProvider类，继承BaseProvider，实现IProvider接口\n2. 修改RunningHubProvider类，同样实现接口和继承基类\n3. 修改SCFProvider类，完成相同改造\n4. 确保现有功能完全保持不变，只是代码结构重构\n5. 为每个Provider添加完整的TypeScript类型定义\n6. 更新单元测试，确保所有测试通过"
}
```

```json
{
  "taskId": "CMS-004",
  "module": "Provider管理",
  "title": "实现Provider管理CRUD接口",
  "phase": "Week 1",
  "estimatedHours": 6,
  "priority": "P0", 
  "dependencies": ["CMS-001"],
  "description": "创建Provider管理的后端REST API接口",
  "technicalRequirements": [
    "GET /api/providers - 获取Provider列表",
    "POST /api/providers - 创建新Provider",
    "PUT /api/providers/:id - 更新Provider",
    "DELETE /api/providers/:id - 删除Provider（软删除）",
    "POST /api/providers/:id/test - 测试Provider连接"
  ],
  "acceptanceCriteria": [
    "所有API接口正常工作，返回正确的HTTP状态码",
    "创建Provider时验证必填字段",
    "软删除机制正确实现（标记为disabled）",
    "测试连接功能真实调用Provider的testConnection方法"
  ],
  "codeLocations": [
    "backend/src/routes/providers.routes.js",
    "backend/src/controllers/providers.controller.js",
    "backend/src/services/providers.service.js"
  ],
  "testStrategy": [
    "API测试：测试所有CRUD接口",
    "验证测试：测试字段验证和错误处理",
    "集成测试：测试与数据库的交互"
  ],
  "aiPromptSuggestion": "请帮我创建Provider管理的Express.js REST API。要求：\n1. 实现完整的CRUD操作（列表、创建、更新、删除、测试连接）\n2. 使用Knex.js进行数据库操作，表名为provider_endpoints\n3. 创建Provider时验证必需字段：name、type、base_url、auth_type\n4. 实现软删除机制，删除时只更新status字段为disabled\n5. 测试连接接口调用对应Provider的testConnection()方法\n6. 使用Joi或类似库进行请求参数验证\n7. 编写API测试用例"
}
```

```json
{
  "taskId": "CMS-005",
  "module": "Provider管理",
  "title": "实现Provider管理前端页面",
  "phase": "Week 1",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-004"],
  "description": "创建Provider管理的前端React页面，支持列表、创建、编辑、删除操作",
  "technicalRequirements": [
    "Provider列表页：表格展示，支持搜索和分页",
    "创建Provider页面：表单包含所有配置字段",
    "编辑Provider页面：预填充表单数据",
    "删除确认对话框",
    "测试连接按钮和结果展示"
  ],
  "acceptanceCriteria": [
    "页面加载速度快，列表数据正确显示",
    "创建和编辑表单验证正确",
    "删除操作有确认提示，软删除成功",
    "测试连接功能正常工作，显示测试结果"
  ],
  "codeLocations": [
    "frontend/src/app/admin/providers/page.tsx",
    "frontend/src/app/admin/providers/create/page.tsx", 
    "frontend/src/app/admin/providers/[id]/edit/page.tsx",
    "frontend/src/components/ProviderForm.tsx"
  ],
  "testStrategy": [
    "功能测试：测试所有页面功能",
    "表单测试：测试表单验证和提交",
    "集成测试：测试与后端API的交互"
  ],
  "aiPromptSuggestion": "请帮我创建Provider管理的Next.js前端页面。要求：\n1. 使用Next.js 14 App Router和React 18\n2. 列表页使用Ant Design Table，支持搜索和分页\n3. 创建和编辑页使用Ant Design Form，包含字段：name、type、base_url、auth_type、credentials等\n4. 实现表单验证，必填字段提示\n5. 集成后端API，实现CRUD操作\n6. 删除操作使用Ant Design Modal确认对话框\n7. 测试连接按钮调用API并显示成功/失败结果\n8. 使用TypeScript确保类型安全"
}
```

```json
{
  "taskId": "CMS-006",
  "module": "Provider管理",
  "title": "实现Provider测试连接功能",
  "phase": "Week 1",
  "estimatedHours": 4,
  "priority": "P1",
  "dependencies": ["CMS-002", "CMS-004"],
  "description": "为Provider添加测试连接功能，验证API端点可用性",
  "technicalRequirements": [
    "在BaseProvider中实现通用的testConnection方法",
    "每个具体Provider实现特定的连接测试逻辑",
    "前端集成测试按钮和结果展示",
    "测试结果缓存和历史记录"
  ],
  "acceptanceCriteria": [
    "testConnection方法正确验证API端点可用性",
    "测试结果准确反映Provider状态",
    "前端及时显示测试进度和结果",
    "测试失败时提供详细的错误信息"
  ],
  "codeLocations": [
    "backend/src/providers/base/BaseProvider.ts",
    "backend/src/controllers/providers.controller.js",
    "frontend/src/components/ProviderForm.tsx"
  ],
  "testStrategy": [
    "单元测试：测试testConnection方法",
    "集成测试：测试完整的测试连接流程",
    "错误测试：测试各种失败场景"
  ],
  "aiPromptSuggestion": "请帮我实现Provider测试连接功能。要求：\n1. 在BaseProvider中实现testConnection()方法，发送简单的HTTP请求验证端点\n2. 为TencentProvider重写testConnection，调用具体的健康检查API\n3. 为RunningHubProvider实现特定的连接测试逻辑\n4. 在Provider管理后端API中添加/test端点\n5. 前端在Provider表单中添加测试连接按钮，调用API并显示结果\n6. 测试成功显示绿色提示，失败显示红色错误信息\n7. 添加测试超时机制（5秒超时）"
}
```

### 表单设计器模块（8个任务卡）

```json
{
  "taskId": "CMS-007",
  "module": "表单设计器",
  "title": "集成Form.io React到项目",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": [],
  "description": "将Form.io React库集成到Next.js项目中，配置基础环境",
  "technicalRequirements": [
    "安装formiojs和@formio/react依赖",
    "配置Form.io主题与Ant Design保持一致",
    "创建基础的表单渲染组件",
    "配置TypeScript类型定义"
  ],
  "acceptanceCriteria": [
    "Form.io库正确安装，无构建错误",
    "基础表单可以正常渲染和提交",
    "主题样式与现有系统一致",
    "TypeScript类型定义完整"
  ],
  "codeLocations": [
    "frontend/package.json",
    "frontend/src/components/form/FormioRenderer.tsx",
    "frontend/src/styles/formio-theme.scss",
    "frontend/src/types/formio.d.ts"
  ],
  "testStrategy": [
    "构建测试：验证安装和构建无错误",
    "渲染测试：测试基础表单渲染",
    "样式测试：验证主题一致性"
  ],
  "aiPromptSuggestion": "请帮我在Next.js项目中集成Form.io React。要求：\n1. 安装formiojs和@formio/react最新版本\n2. 创建FormioRenderer组件，接收JSON schema并渲染表单\n3. 配置自定义CSS主题，使Form.io样式与Ant Design保持一致\n4. 解决可能的SSR问题（Next.js兼容性）\n5. 添加TypeScript类型定义文件\n6. 创建示例页面展示基础表单功能\n7. 测试表单提交和数据收集功能"
}
```

```json
{
  "taskId": "CMS-008",
  "module": "表单设计器", 
  "title": "实现表单设计器UI框架",
  "phase": "Week 2",
  "estimatedHours": 12,
  "priority": "P0",
  "dependencies": ["CMS-007"],
  "description": "创建拖拽式表单设计器的主要UI框架",
  "technicalRequirements": [
    "左侧字段面板：展示10种可拖拽字段类型",
    "中间设计画布：接收拖拽字段并排列",
    "右侧配置面板：字段属性配置",
    "顶部工具栏：保存、预览、清空等操作"
  ],
  "acceptanceCriteria": [
    "字段可以从左侧拖拽到画布",
    "画布中的字段可以选中和配置",
    "配置面板实时响应字段选择",
    "整体布局合理，用户体验流畅"
  ],
  "codeLocations": [
    "frontend/src/components/form-builder/FormBuilder.tsx",
    "frontend/src/components/form-builder/FieldPalette.tsx",
    "frontend/src/components/form-builder/DesignCanvas.tsx",
    "frontend/src/components/form-builder/ConfigPanel.tsx"
  ],
  "testStrategy": [
    "拖拽测试：测试字段拖拽功能",
    "交互测试：测试画布字段选择和管理",
    "配置测试：测试配置面板功能"
  ],
  "aiPromptSuggestion": "请帮我创建拖拽式表单设计器的React组件。要求：\n1. 使用React DnD实现拖拽功能\n2. 左侧FieldPalette展示10种字段类型（image/text/number/select/radio/checkbox/slider/color/date/textarea）\n3. 中间DesignCanvas接收拖拽的字段，支持选择、排序、删除\n4. 右侧ConfigPanel根据选中字段显示对应配置项\n5. 使用Ant Design组件保持UI一致性\n6. 实现字段的序列化存储（维护fields数组）\n7. 添加键盘快捷键支持（删除、复制等）"
}
```

```json
{
  "taskId": "CMS-009",
  "module": "表单设计器",
  "title": "实现字段配置面板",
  "phase": "Week 2",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-008"],
  "description": "为每种字段类型实现详细的配置选项",
  "technicalRequirements": [
    "通用配置：key、label、placeholder、required、helpText",
    "类型特定配置：每种字段类型的特殊选项",
    "验证规则配置：required、min、max、pattern等",
    "实时配置预览"
  ],
  "acceptanceCriteria": [
    "每种字段类型都有完整的配置选项",
    "配置变更实时反映到画布预览",
    "验证规则配置直观易用",
    "配置界面用户体验良好"
  ],
  "codeLocations": [
    "frontend/src/components/form-builder/field-configs/",
    "frontend/src/components/form-builder/field-configs/TextFieldConfig.tsx",
    "frontend/src/components/form-builder/field-configs/ImageFieldConfig.tsx",
    "frontend/src/components/form-builder/field-configs/SelectFieldConfig.tsx"
  ],
  "testStrategy": [
    "配置测试：测试每种字段类型的配置功能",
    "联动测试：测试配置与画布的实时联动",
    "验证测试：测试验证规则配置"
  ],
  "aiPromptSuggestion": "请帮我实现表单字段的配置面板组件。要求：\n1. 为10种字段类型分别创建配置组件\n2. 通用配置包含：字段key、标签、占位符、是否必填、帮助文本\n3. 类型特定配置：\n   - ImageField：最大尺寸、允许格式、多选\n   - SelectField：选项列表（label/value）、默认值\n   - NumberField：最小值、最大值、步长\n   - 等等...\n4. 验证规则配置使用Ant Design Form\n5. 配置变更通过React Context或状态管理实时同步到画布\n6. 使用TypeScript确保配置数据的类型安全"
}
```

```json
{
  "taskId": "CMS-010",
  "module": "表单设计器",
  "title": "实现表单实时预览功能",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-007", "CMS-008"],
  "description": "在设计器中实现表单的实时预览，所见即所得",
  "technicalRequirements": [
    "预览面板与设计画布同步",
    "支持预览模式切换（设计模式/预览模式）",
    "预览表单可交互测试",
    "响应式布局预览"
  ],
  "acceptanceCriteria": [
    "设计画布的变更实时反映到预览",
    "预览表单可以正常填写和交互",
    "模式切换流畅无闪烁",
    "移动端预览效果正确"
  ],
  "codeLocations": [
    "frontend/src/components/form-builder/FormPreview.tsx",
    "frontend/src/hooks/useFormPreview.ts",
    "frontend/src/components/form-builder/PreviewModeToggle.tsx"
  ],
  "testStrategy": [
    "同步测试：测试设计与预览的实时同步",
    "交互测试：测试预览表单的交互功能",
    "响应式测试：测试不同屏幕尺寸的预览效果"
  ],
  "aiPromptSuggestion": "请帮我实现表单设计器的实时预览功能。要求：\n1. 创建FormPreview组件，使用Form.io渲染器显示当前设计的表单\n2. 实现useFormPreview Hook管理预览状态\n3. 添加模式切换按钮（设计模式/预览模式）\n4. 在设计模式中，右侧显示配置面板；在预览模式中，右侧显示实际表单\n5. 确保设计变更实时同步到预览（使用React state或Context）\n6. 预览表单支持真实交互：填写、提交、验证\n7. 添加移动端预览模式，显示不同屏幕尺寸下的效果"
}
```

```json
{
  "taskId": "CMS-011",
  "module": "表单设计器",
  "title": "实现JSON Schema生成器",
  "phase": "Week 2", 
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-008", "CMS-009"],
  "description": "将UI设计的表单转换为标准的JSON Schema格式",
  "technicalRequirements": [
    "将fields数组转换为Form.io兼容的JSON Schema",
    "包含完整的验证规则和UI配置",
    "支持schema版本管理",
    "schema格式验证和错误提示"
  ],
  "acceptanceCriteria": [
    "生成的JSON Schema符合Form.io标准",
    "所有字段配置正确转换",
    "验证规则完整保留",
    "schema可以通过Form.io正确渲染"
  ],
  "codeLocations": [
    "frontend/src/utils/schema-generator.ts",
    "frontend/src/utils/schema-validator.ts",
    "frontend/src/types/form-schema.ts"
  ],
  "testStrategy": [
    "生成测试：测试各种字段组合的schema生成",
    "验证测试：测试生成schema的格式正确性",
    "往返测试：测试schema→设计→schema的完整性"
  ],
  "aiPromptSuggestion": "请帮我实现JSON Schema生成器工具函数。要求：\n1. 创建schemaGenerator函数，将fields数组转换为Form.io JSON Schema\n2. 支持10种字段类型的完整配置转换\n3. 包含验证规则转换：required → validation.required, min/max → validation.min/max\n4. 实现schemaValidator函数，验证生成schema的合法性\n5. 添加TypeScript接口定义FormSchema\n6. 创建单元测试，覆盖各种字段组合的生成场景\n7. 实现schema的版本控制，支持向后兼容"
}
```

```json
{
  "taskId": "CMS-012",
  "module": "表单设计器",
  "title": "实现表单数据持久化",
  "phase": "Week 2",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-010", "CMS-011"],
  "description": "将设计的表单schema保存到数据库，支持读取和更新",
  "technicalRequirements": [
    "创建form_schemas表的CRUD接口",
    "前端集成保存和加载功能",
    "支持schema版本管理",
    "自动保存草稿功能"
  ],
  "acceptanceCriteria": [
    "表单设计可以成功保存到数据库",
    "保存的表单可以重新加载编辑",
    "版本管理功能正常工作",
    "自动草稿保存防止数据丢失"
  ],
  "codeLocations": [
    "backend/src/routes/form-schemas.routes.js",
    "backend/src/controllers/form-schemas.controller.js",
    "frontend/src/hooks/useFormSave.ts"
  ],
  "testStrategy": [
    "持久化测试：测试schema的保存和加载",
    "版本测试：测试版本管理功能",
    "草稿测试：测试自动保存功能"
  ],
  "aiPromptSuggestion": "请帮我实现表单schema的持久化功能。要求：\n1. 创建form_schemas表的Express.js CRUD API\n2. 前端实现保存功能，将生成的JSON Schema发送到后端\n3. 实现加载功能，从数据库读取schema并还原到设计器\n4. 添加版本管理，每次保存创建新版本\n5. 实现自动草稿保存，每30秒自动保存当前进度\n6. 添加保存状态提示（保存中、保存成功、保存失败）\n7. 处理并发编辑冲突（乐观锁机制）"
}
```

```json
{
  "taskId": "CMS-013",
  "module": "表单设计器",
  "title": "实现高级字段类型（对象、数组）",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P1",
  "dependencies": ["CMS-008"],
  "description": "支持对象和数组等复杂字段类型，实现嵌套表单设计",
  "technicalRequirements": [
    "对象字段：支持字段分组和嵌套",
    "数组字段：支持动态添加/删除子项",
    "嵌套字段的拖拽和配置",
    "复杂字段的预览和渲染"
  ],
  "acceptanceCriteria": [
    "对象字段可以正常添加和配置子字段",
    "数组字段支持动态增减项",
    "嵌套字段的拖拽体验流畅",
    "复杂字段在预览中正确渲染"
  ],
  "codeLocations": [
    "frontend/src/components/form-builder/field-configs/ObjectFieldConfig.tsx",
    "frontend/src/components/form-builder/field-configs/ArrayFieldConfig.tsx",
    "frontend/src/components/form-builder/fields/ObjectField.tsx",
    "frontend/src/components/form-builder/fields/ArrayField.tsx"
  ],
  "testStrategy": [
    "功能测试：测试对象和数组字段的完整功能",
    "嵌套测试：测试多层嵌套场景",
    "性能测试：测试复杂表单的渲染性能"
  ],
  "aiPromptSuggestion": "请帮我实现对象和数组高级字段类型。要求：\n1. 创建ObjectField组件，支持字段分组和嵌套\n2. 创建ArrayField组件，支持动态添加、删除、排序子项\n3. 实现对象字段的配置面板，可以添加和管理子字段\n4. 实现数组字段的配置面板，配置子项的类型和验证\n5. 在设计画布中支持嵌套字段的拖拽和缩进显示\n6. 确保预览模式正确渲染复杂字段\n7. 添加性能优化，防止嵌套过深导致的渲染问题"
}
```

```json
{
  "taskId": "CMS-014",
  "module": "表单设计器",
  "title": "优化大表单性能",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-008"],
  "description": "针对字段数量多的表单进行性能优化",
  "technicalRequirements": [
    "虚拟滚动：只渲染可见区域的字段",
    "字段懒加载：非激活字段延迟渲染",
    "配置面板优化：减少不必要的重渲染",
    "内存泄漏防护"
  ],
  "acceptanceCriteria": [
    "100+字段的表单设计流畅无卡顿",
    "内存使用稳定，无泄漏",
    "滚动体验平滑",
    "配置响应及时"
  ],
  "codeLocations": [
    "frontend/src/components/form-builder/VirtualizedCanvas.tsx",
    "frontend/src/hooks/useVirtualScroll.ts",
    "frontend/src/utils/performance-optimizer.ts"
  ],
  "testStrategy": [
    "性能测试：测试大量字段的渲染性能",
    "内存测试：监控内存使用情况",
    "体验测试：测试用户交互的流畅度"
  ],
  "aiPromptSuggestion": "请帮我优化表单设计器的性能。要求：\n1. 使用react-window实现虚拟滚动，只渲染可见区域的字段\n2. 实现字段懒加载，非激活区域的字段延迟渲染\n3. 使用React.memo优化配置面板，避免不必要的重渲染\n4. 使用useCallback和useMemo优化回调函数和计算值\n5. 添加性能监控，记录渲染时间和帧率\n6. 实现内存泄漏检测和清理机制\n7. 测试100个字段的大表单，确保设计体验流畅"
}
```

### 流程编辑器模块（8个任务卡）

```json
{
  "taskId": "CMS-015",
  "module": "流程编辑器",
  "title": "集成React Flow到项目",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "将React Flow流程图库集成到项目中，配置基础环境",
  "technicalRequirements": [
    "安装reactflow依赖",
    "配置基础流程图画布",
    "定义节点和连线类型",
    "配置与Ant Design一致的主题"
  ],
  "acceptanceCriteria": [
    "React Flow正确安装，无构建错误",
    "基础画布可以正常显示和交互",
    "节点拖拽和连线功能正常",
    "主题样式与现有系统一致"
  ],
  "codeLocations": [
    "frontend/package.json",
    "frontend/src/components/pipeline-editor/PipelineCanvas.tsx",
    "frontend/src/components/pipeline-editor/nodes/BaseNode.tsx",
    "frontend/src/components/pipeline-editor/edges/BaseEdge.tsx"
  ],
  "testStrategy": [
    "基础测试：测试画布基础功能",
    "交互测试：测试节点拖拽和连线",
    "样式测试：验证主题一致性"
  ],
  "aiPromptSuggestion": "请帮我在Next.js项目中集成React Flow。要求：\n1. 安装reactflow最新版本\n2. 创建PipelineCanvas组件，配置基础画布\n3. 定义4种节点类型：API调用、云函数、条件判断、并行执行\n4. 实现自定义节点样式，与Ant Design保持一致\n5. 配置连线样式和交互\n6. 实现画布的缩放、拖拽、多选等基础功能\n7. 添加TypeScript类型定义\n8. 创建示例流程展示功能"
}
```

```json
{
  "taskId": "CMS-016",
  "module": "流程编辑器",
  "title": "实现节点设计器",
  "phase": "Week 3",
  "estimatedHours": 10,
  "priority": "P0",
  "dependencies": ["CMS-015"],
  "description": "为每种节点类型实现详细的设计器和配置面板",
  "technicalRequirements": [
    "API调用节点：选择Provider、配置参数、超时重试",
    "云函数节点：选择函数、配置入参",
    "条件判断节点：配置条件表达式和分支",
    "并行执行节点：配置并行分支"
  ],
  "acceptanceCriteria": [
    "每种节点都有完整的配置选项",
    "节点配置直观易用",
    "配置变更实时反映到节点显示",
    "复杂的节点配置也能清晰表达"
  ],
  "codeLocations": [
    "frontend/src/components/pipeline-editor/nodes/ApiNode.tsx",
    "frontend/src/components/pipeline-editor/nodes/FunctionNode.tsx",
    "frontend/src/components/pipeline-editor/nodes/ConditionNode.tsx",
    "frontend/src/components/pipeline-editor/nodes/ParallelNode.tsx",
    "frontend/src/components/pipeline-editor/config-panels/"
  ],
  "testStrategy": [
    "节点测试：测试每种节点的设计和配置",
    "配置测试：测试配置面板的功能",
    "联动测试：测试节点与配置的实时联动"
  ],
  "aiPromptSuggestion": "请帮我实现React Flow的节点设计器。要求：\n1. 创建4种自定义节点组件：ApiNode、FunctionNode、ConditionNode、ParallelNode\n2. 每种节点有独特的视觉设计和图标\n3. 实现节点配置面板，根据节点类型显示不同配置项：\n   - ApiNode：Provider选择、请求参数、超时、重试\n   - FunctionNode：函数选择、输入参数映射\n   - ConditionNode：条件表达式配置、分支管理\n   - ParallelNode：并行分支配置\n4. 使用Ant Design Form进行配置\n5. 节点选中时显示配置面板，配置变更实时更新节点标签\n6. 实现节点的拖拽、复制、删除功能"
}
```

```json
{
  "taskId": "CMS-017",
  "module": "流程编辑器",
  "title": "实现连线逻辑和流程验证",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-016"],
  "description": "实现节点间的连线逻辑和流程合法性验证",
  "technicalRequirements": [
    "连线规则：限制某些节点类型的连接",
    "流程验证：检测循环依赖、孤立节点",
    "连线配置：条件连线的表达式配置",
    "实时验证提示"
  ],
  "acceptanceCriteria": [
    "连线规则正确执行，阻止非法连接",
    "循环依赖被检测并提示",
    "验证错误清晰显示",
    "用户体验流畅，错误提示友好"
  ],
  "codeLocations": [
    "frontend/src/components/pipeline-editor/validation/FlowValidator.ts",
    "frontend/src/components/pipeline-editor/validation/CycleDetector.ts",
    "frontend/src/components/pipeline-editor/hooks/useConnectionRules.ts"
  ],
  "testStrategy": [
    "规则测试：测试连线规则的正确性",
    "验证测试：测试各种流程结构的验证",
    "错误测试：测试错误情况的处理和提示"
  ],
  "aiPromptSuggestion": "请帮我实现流程编辑器的连线逻辑和验证。要求：\n1. 实现连线规则：条件节点只能有1个输入、2个输出；并行节点可以有多个输入输出等\n2. 使用图算法检测循环依赖（拓扑排序或DFS）\n3. 检测孤立节点（没有连接到的节点）\n4. 实现实时验证，错误时在画布上显示红色提示\n5. 条件连线支持配置条件表达式\n6. 添加连线标签，显示条件描述\n7. 实现验证结果的汇总显示，列出所有问题\n8. 阻止不合法的连线操作，并给出原因提示"
}
```

```json
{
  "taskId": "CMS-018",
  "module": "流程编辑器",
  "title": "实现Pipeline JSON生成器",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-016"],
  "description": "将流程图转换为Pipeline JSON Schema",
  "technicalRequirements": [
    "将节点和连线转换为pipeline_schemas格式",
    "包含完整的步骤配置和执行顺序",
    "支持变量引用和参数映射",
    "schema格式验证"
  ],
  "acceptanceCriteria": [
    "生成的JSON Schema符合pipeline_schemas表标准",
    "所有节点配置正确转换",
    "执行顺序和依赖关系正确保留",
    "schema可以通过Pipeline引擎执行"
  ],
  "codeLocations": [
    "frontend/src/utils/pipeline-generator.ts",
    "frontend/src/utils/pipeline-validator.ts",
    "frontend/src/types/pipeline-schema.ts"
  ],
  "testStrategy": [
    "生成测试：测试各种流程结构的schema生成",
    "验证测试：测试生成schema的格式正确性",
    "往返测试：测试schema→流程图→schema的完整性"
  ],
  "aiPromptSuggestion": "请帮我实现Pipeline JSON生成器。要求：\n1. 创建pipelineGenerator函数，将React Flow图数据转换为Pipeline JSON Schema\n2. 转换节点为steps数组，保持执行顺序\n3. 转换连线为nextSteps和errorSteps引用\n4. 处理条件节点的分支逻辑\n5. 处理并行节点的分支配置\n6. 实现pipelineValidator函数，验证生成schema的合法性\n7. 添加TypeScript接口定义PipelineSchema\n8. 创建单元测试，覆盖各种流程结构的生成场景"
}
```

```json
{
  "taskId": "CMS-019",
  "module": "流程编辑器",
  "title": "实现Pipeline测试运行器",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-001", "CMS-018"],
  "description": "实现Pipeline的测试执行功能，验证流程正确性",
  "technicalRequirements": [
    "测试数据输入界面",
    "实时执行状态可视化",
    "步骤执行结果展示",
    "错误信息和日志输出"
  ],
  "acceptanceCriteria": [
    "测试数据可以正确输入",
    "执行状态实时可视化显示",
    "每个步骤的执行结果清晰可见",
    "错误信息详细可排查"
  ],
  "codeLocations": [
    "frontend/src/components/pipeline-editor/PipelineTester.tsx",
    "frontend/src/hooks/usePipelineTest.ts",
    "backend/src/services/pipelineTest.service.js"
  ],
  "testStrategy": [
    "功能测试：测试完整的测试执行流程",
    "状态测试：测试各种执行状态的显示",
    "错误测试：测试错误情况的处理和显示"
  ],
  "aiPromptSuggestion": "请帮我实现Pipeline测试运行器。要求：\n1. 创建PipelineTester组件，提供测试数据输入表单\n2. 实现usePipelineTest Hook，管理测试执行状态\n3. 在后端创建pipelineTest服务，调用Pipeline引擎执行测试\n4. 实时可视化显示执行状态：等待中、执行中、成功、失败\n5. 显示每个步骤的输入、输出、执行时间\n6. 提供详细的执行日志和错误信息\n7. 支持停止执行和重新测试\n8. 测试结果可以保存为测试用例"
}
```

```json
{
  "taskId": "CMS-020",
  "module": "流程编辑器",
  "title": "实现变量系统和参数映射",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P1",
  "dependencies": ["CMS-016"],
  "description": "实现Pipeline变量系统和节点间的参数映射",
  "technicalRequirements": [
    "全局变量定义和管理",
    "步骤间参数引用：{{stepName.output}}",
    "参数映射可视化配置",
    "变量类型验证"
  ],
  "acceptanceCriteria": [
    "变量可以正确定义和引用",
    "参数映射直观易配置",
    "类型验证防止错误引用",
    "变量引用在测试时正确解析"
  ],
  "codeLocations": [
    "frontend/src/components/pipeline-editor/VariablePanel.tsx",
    "frontend/src/utils/variable-parser.ts",
    "frontend/src/hooks/useVariableMapping.ts"
  ],
  "testStrategy": [
    "变量测试：测试变量定义和引用",
    "映射测试：测试参数映射功能",
    "验证测试：测试变量类型验证",
    "解析测试：测试变量引用解析"
  ],
  "aiPromptSuggestion": "请帮我实现Pipeline变量系统和参数映射。要求：\n1. 创建VariablePanel组件，管理全局变量（名称、类型、默认值）\n2. 实现变量引用语法：{{stepId.output.field}} 和 {{variables.varName}}\n3. 在节点配置中支持参数映射，使用类JSONPath语法\n4. 实现变量类型验证，防止类型不匹配\n5. 添加变量自动补全功能，在输入时提示可用变量\n6. 实现变量解析器，在测试执行时解析变量引用\n7. 可视化显示变量依赖关系\n8. 创建变量使用情况分析，显示哪些步骤使用了哪些变量"
}
```

```json
{
  "taskId": "CMS-021",
  "module": "流程编辑器",
  "title": "实现流程模板和复用",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-018"],
  "description": "实现流程模板库，支持常用流程的复用",
  "technicalRequirements": [
    "模板库管理：常用流程模式",
    "模板导入导出功能",
    "子流程封装和复用",
    "模板版本管理"
  ],
  "acceptanceCriteria": [
    "模板可以保存和加载",
    "导入导出功能正常工作",
    "子流程封装正确实现",
    "版本管理功能完整"
  ],
  "codeLocations": [
    "frontend/src/components/pipeline-editor/TemplateLibrary.tsx",
    "frontend/src/utils/pipeline-template.ts",
    "backend/src/routes/pipeline-templates.routes.js"
  ],
  "testStrategy": [
    "模板测试：测试模板的保存和加载",
    "导入导出测试：测试模板交换功能",
    "子流程测试：测试子流程的封装和调用"
  ],
  "aiPromptSuggestion": "请帮我实现流程模板和复用功能。要求：\n1. 创建TemplateLibrary组件，展示可用的流程模板\n2. 实现模板保存功能，将当前流程保存为模板\n3. 实现模板加载功能，将模板应用到当前画布\n4. 支持子流程封装，将一组节点封装为可复用的子流程\n5. 实现模板的导入导出（JSON格式）\n6. 添加模板分类和标签管理\n7. 实现模板版本历史，支持回滚到旧版本\n8. 提供常用模板：简单API调用、条件分支、并行处理等"
}
```

```json
{
  "taskId": "CMS-022",
  "module": "流程编辑器",
  "title": "优化大流程性能",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-015"],
  "description": "针对节点数量多的流程进行性能优化",
  "technicalRequirements": [
    "画布虚拟化：只渲染可见区域的节点",
    "节点懒加载：非激活节点简化渲染",
    "连线优化：减少不必要的重计算",
    "内存管理：及时清理未使用的资源"
  ],
  "acceptanceCriteria": [
    "50+节点的流程编辑流畅",
    "内存使用稳定，无泄漏",
    "缩放和拖拽体验平滑",
    "复杂流程响应及时"
  ],
  "codeLocations": [
    "frontend/src/components/pipeline-editor/optimization/",
    "frontend/src/hooks/useFlowPerformance.ts",
    "frontend/src/utils/flow-optimizer.ts"
  ],
  "testStrategy": [
    "性能测试：测试大量节点的渲染性能",
    "内存测试：监控内存使用情况",
    "压力测试：测试复杂流程的极限性能"
  ],
  "aiPromptSuggestion": "请帮我优化流程编辑器的性能。要求：\n1. 实现画布虚拟化，只渲染视口内的节点\n2. 非激活节点使用简化渲染（只显示标题）\n3. 优化连线计算，使用空间索引加速查找\n4. 使用React.memo优化节点组件重渲染\n5. 实现增量更新，只更新变更的节点\n6. 添加性能监控面板，显示帧率和节点数量\n7. 实现内存泄漏检测，及时清理事件监听器\n8. 测试50个节点的大流程，确保编辑体验流畅"
}
```

### Prompt管理模块（4个任务卡）

```json
{
  "taskId": "CMS-023",
  "module": "Prompt管理",
  "title": "集成Monaco Editor到项目",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": [],
  "description": "将Monaco代码编辑器集成到项目中，配置Prompt编辑环境",
  "technicalRequirements": [
    "安装@monaco-editor/react依赖",
    "配置TypeScript和JavaScript语法支持",
    "配置自定义语法高亮（Prompt变量）",
    "解决Next.js SSR兼容性问题"
  ],
  "acceptanceCriteria": [
    "Monaco Editor正确安装，无构建错误",
    "代码编辑功能完整",
    "自定义语法高亮正常工作",
    "SSR兼容性问题已解决"
  ],
  "codeLocations": [
    "frontend/package.json",
    "frontend/src/components/prompt-editor/PromptEditor.tsx",
    "frontend/src/utils/monaco-config.ts",
    "frontend/src/types/monaco.d.ts"
  ],
  "testStrategy": [
    "编辑测试：测试代码编辑基础功能",
    "语法测试：测试自定义语法高亮",
    "兼容性测试：测试Next.js SSR兼容性"
  ],
  "aiPromptSuggestion": "请帮我在Next.js项目中集成Monaco Editor。要求：\n1. 安装@monaco-editor/react最新版本\n2. 创建PromptEditor组件，支持代码编辑\n3. 配置TypeScript和JavaScript语言支持\n4. 添加自定义语法高亮，为{{变量名}}添加特殊颜色\n5. 解决SSR问题：使用dynamic import或配置webpack\n6. 配置编辑器主题，与Ant Design保持一致\n7. 实现基础编辑功能：撤销重做、查找替换、代码折叠\n8. 添加编辑器大小调整功能"
}
```

```json
{
  "taskId": "CMS-024",
  "module": "Prompt管理",
  "title": "实现Prompt变量替换引擎",
  "phase": "Week 3",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": ["CMS-023"],
  "description": "使用Handlebars.js实现Prompt变量替换和实时预览",
  "technicalRequirements": [
    "集成Handlebars.js模板引擎",
    "变量提取：从Prompt文本识别所有变量",
    "实时预览：输入变量值，查看渲染结果",
    "错误处理：模板语法错误提示"
  ],
  "acceptanceCriteria": [
    "变量替换正确执行",
    "变量提取准确完整",
    "实时预览响应及时",
    "错误提示清晰有用"
  ],
  "codeLocations": [
    "frontend/src/utils/prompt-engine.ts",
    "frontend/src/components/prompt-editor/VariablePanel.tsx",
    "frontend/src/components/prompt-editor/PreviewPanel.tsx"
  ],
  "testStrategy": [
    "替换测试：测试各种变量替换场景",
    "提取测试：测试变量识别的准确性",
    "预览测试：测试实时预览功能",
    "错误测试：测试错误处理机制"
  ],
  "aiPromptSuggestion": "请帮我实现Prompt变量替换引擎。要求：\n1. 集成Handlebars.js模板引擎\n2. 实现变量提取函数，从Prompt文本识别所有{{variable}}格式的变量\n3. 创建VariablePanel组件，列出所有变量并提供输入框\n4. 实现实时预览，变量输入后立即显示渲染结果\n5. 添加Handlebars帮助函数，支持条件判断和循环\n6. 实现模板语法验证，错误时高亮显示问题位置\n7. 支持变量默认值配置\n8. 创建单元测试，覆盖各种模板替换场景"
}
```

```json
{
  "taskId": "CMS-025",
  "module": "Prompt管理",
  "title": "实现Prompt测试和AI调用",
  "phase": "Week 3",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": ["CMS-024"],
  "description": "实现Prompt测试功能，支持调用真实AI API验证效果",
  "technicalRequirements": [
    "AI提供商选择（OpenAI、Azure、自定义）",
    "测试参数配置（temperature、max_tokens等）",
    "实时调用和流式响应",
    "测试结果保存和对比"
  ],
  "acceptanceCriteria": [
    "可以成功调用AI API",
    "流式响应实时显示",
    "测试参数正确传递",
    "测试结果可以保存和复用"
  ],
  "codeLocations": [
    "frontend/src/components/prompt-editor/PromptTester.tsx",
    "frontend/src/hooks/useAITest.ts",
    "backend/src/services/aiTest.service.js"
  ],
  "testStrategy": [
    "调用测试：测试AI API调用功能",
    "流式测试：测试流式响应显示",
    "参数测试：测试各种参数配置",
    "保存测试：测试结果保存功能"
  ],
  "aiPromptSuggestion": "请帮我实现Prompt测试和AI调用功能。要求：\n1. 创建PromptTester组件，提供AI提供商选择和参数配置\n2. 实现useAITest Hook，管理AI调用状态\n3. 在后端创建aiTest服务，调用各种AI提供商API\n4. 支持流式响应，实时显示生成内容\n5. 配置常用参数：temperature、max_tokens、top_p等\n6. 保存测试记录，包括输入、输出、参数配置\n7. 支持测试结果对比，可以同时测试多个Prompt版本\n8. 添加使用量统计和限制"
}
```

```json
{
  "taskId": "CMS-026",
  "module": "Prompt管理",
  "title": "实现Prompt分类和版本管理",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P1",
  "dependencies": ["CMS-023"],
  "description": "实现Prompt分类管理和版本历史功能",
  "technicalRequirements": [
    "分类管理：创建、编辑、删除分类",
    "版本历史：保存每次修改记录",
    "版本对比：比较不同版本的差异",
    "版本回滚：恢复到历史版本"
  ],
  "acceptanceCriteria": [
    "分类管理功能完整",
    "版本历史正确记录",
    "版本对比清晰直观",
    "版本回滚正确执行"
  ],
  "codeLocations": [
    "frontend/src/components/prompt-editor/CategoryManager.tsx",
    "frontend/src/components/prompt-editor/VersionHistory.tsx",
    "backend/src/routes/prompt-categories.routes.js"
  ],
  "testStrategy": [
    "分类测试：测试分类管理功能",
    "版本测试：测试版本历史记录",
    "对比测试：测试版本对比功能",
    "回滚测试：测试版本回滚功能"
  ],
  "aiPromptSuggestion": "请帮我实现Prompt分类和版本管理。要求：\n1. 创建CategoryManager组件，管理Prompt分类\n2. 实现VersionHistory组件，显示版本列表和对比\n3. 在后端创建prompt_versions表，存储历史版本\n4. 每次保存Prompt时自动创建新版本\n5. 实现版本对比功能，高亮显示差异\n6. 支持版本回滚，恢复到此版本内容\n7. 添加版本备注功能，记录修改原因\n8. 实现分类的拖拽排序和嵌套分类"
}
```

### 内容管理模块（4个任务卡）

```json
{
  "taskId": "CMS-027",
  "module": "内容管理",
  "title": "实现公告管理功能",
  "phase": "Week 4",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "实现公告的创建、编辑、发布、下架全流程管理",
  "technicalRequirements": [
    "公告列表：搜索、筛选、分页",
    "公告表单：富文本编辑器、目标用户选择、定时发布",
    "状态管理：草稿、已发布、已过期",
    "前端展示：根据公告类型正确显示"
  ],
  "acceptanceCriteria": [
    "公告CRUD功能完整",
    "富文本编辑器正常工作",
    "定时发布准确执行",
    "前端正确显示各类公告"
  ],
  "codeLocations": [
    "frontend/src/app/admin/announcements/page.tsx",
    "frontend/src/app/admin/announcements/[id]/edit/page.tsx",
    "frontend/src/components/announcements/AnnouncementForm.tsx",
    "backend/src/routes/announcements.routes.js"
  ],
  "testStrategy": [
    "功能测试：测试公告完整管理流程",
    "定时测试：测试定时发布功能",
    "显示测试：测试前端公告显示"
  ],
  "aiPromptSuggestion": "请帮我实现公告管理功能。要求：\n1. 使用Ant Design ProTable实现公告列表，支持搜索和筛选\n2. 创建AnnouncementForm表单，包含标题、内容、类型、目标用户等字段\n3. 集成富文本编辑器（如@uiw/react-md-editor）\n4. 实现定时发布功能，使用cron job或setTimeout\n5. 公告状态自动管理：定时发布、自动过期\n6. 前端根据公告类型正确显示：弹窗、横幅、通知\n7. 目标用户筛选正确工作：全体用户、会员、新用户等\n8. 添加公告预览功能"
}
```

```json
{
  "taskId": "CMS-028",
  "module": "内容管理",
  "title": "实现轮播图管理功能",
  "phase": "Week 4",
  "estimatedHours": 8,
  "priority": "P0",
  "dependencies": [],
  "description": "实现轮播图的上传、排序、定时上下线管理",
  "technicalRequirements": [
    "图片上传：腾讯云COS集成",
    "拖拽排序：React DnD实现",
    "定时上下线：立即上线/指定时间",
    "前端轮播：集成Swiper或Ant Design Carousel"
  ],
  "acceptanceCriteria": [
    "图片上传成功到COS",
    "拖拽排序流畅准确",
    "定时功能正确执行",
    "前端轮播效果良好"
  ],
  "codeLocations": [
    "frontend/src/app/admin/banners/page.tsx",
    "frontend/src/components/banners/BannerForm.tsx",
    "frontend/src/components/banners/BannerSorter.tsx",
    "backend/src/routes/banners.routes.js"
  ],
  "testStrategy": [
    "上传测试：测试图片上传功能",
    "排序测试：测试拖拽排序功能",
    "定时测试：测试定时上下线",
    "轮播测试：测试前端轮播效果"
  ],
  "aiPromptSuggestion": "请帮我实现轮播图管理功能。要求：\n1. 创建BannerForm表单，支持图片上传、标题、链接等字段\n2. 集成腾讯云COS上传，显示上传进度和预览\n3. 使用React DnD实现拖拽排序功能\n4. 实现定时上下线，立即上线或指定时间\n5. 在前端集成Swiper轮播组件，显示轮播图\n6. 添加轮播图点击统计功能\n7. 支持移动端和PC端不同的轮播图配置\n8. 实现轮播图缓存，提高加载速度"
}
```

```json
{
  "taskId": "CMS-029",
  "module": "内容管理",
  "title": "实现会员套餐配置",
  "phase": "Week 4",
  "estimatedHours": 10,
  "priority": "P0",
  "dependencies": [],
  "description": "实现会员套餐的配置和管理，包括价格、配额、权限",
  "technicalRequirements": [
    "套餐表单：价格配置、配额设置、功能权限",
    "拖拽排序：套餐显示顺序",
    "权限管理：从feature_definitions读取可用功能",
    "购买流程集成"
  ],
  "acceptanceCriteria": [
    "套餐CRUD功能完整",
    "价格和配额配置正确",
    "权限选择正常工作",
    "购买流程集成成功"
  ],
  "codeLocations": [
    "frontend/src/app/admin/membership/page.tsx",
    "frontend/src/components/membership/MembershipForm.tsx",
    "frontend/src/components/membership/PermissionSelector.tsx",
    "backend/src/routes/membership.routes.js"
  ],
  "testStrategy": [
    "功能测试：测试套餐完整管理流程",
    "权限测试：测试功能权限配置",
    "购买测试：测试购买流程集成",
    "排序测试：测试拖拽排序功能"
  ],
  "aiPromptSuggestion": "请帮我实现会员套餐配置功能。要求：\n1. 创建MembershipForm表单，配置名称、价格、配额、有效期等\n2. 实现价格配置：原价、现价、折扣计算\n3. 创建PermissionSelector组件，从feature_definitions表读取功能列表\n4. 使用React DnD实现套餐拖拽排序\n5. 集成到现有购买流程，确保套餐购买正常工作\n6. 添加套餐推荐标签配置（最超值、热门等）\n7. 实现套餐权限验证，用户只能访问已购买的功能\n8. 添加套餐销售统计和图表"
}
```

```json
{
  "taskId": "CMS-030",
  "module": "内容管理",
  "title": "实现文案配置管理",
  "phase": "Week 4",
  "estimatedHours": 6,
  "priority": "P0",
  "dependencies": [],
  "description": "实现多页面文案的动态配置管理",
  "technicalRequirements": [
    "按页面分组：首页、工作台、会员中心等",
    "文案表单：键值对配置，支持多语言",
    "实时生效：配置更新后前端立即更新",
    "版本管理：文案修改历史"
  ],
  "acceptanceCriteria": [
    "文案配置界面直观易用",
    "多页面分组清晰",
    "配置实时生效",
    "版本管理完整"
  ],
  "codeLocations": [
    "frontend/src/app/admin/copywriting/page.tsx",
    "frontend/src/components/copywriting/CopywritingForm.tsx",
    "frontend/src/hooks/useCopywriting.ts",
    "backend/src/routes/copywriting.routes.js"
  ],
  "testStrategy": [
    "配置测试：测试文案配置功能",
    "实时测试：测试配置实时生效",
    "分组测试：测试页面分组功能",
    "版本测试：测试版本管理"
  ],
  "aiPromptSuggestion": "请帮我实现文案配置管理。要求：\n1. 创建CopywritingForm表单，按页面分组管理文案\n2. 扩展system_configs表，添加page、locale字段\n3. 实现文案的键值对配置，支持文本、富文本等类型\n4. 在前端使用useCopywriting Hook动态加载文案\n5. 实现文案实时更新，配置修改后前端立即生效\n6. 添加多语言支持，默认中文，可扩展英文\n7. 实现文案版本历史，记录每次修改\n8. 提供文案导出导入功能，方便迁移"
}
```

## 总结

这个实施规划为您的CMS自研项目提供了完整的技术架构、详细的工作计划和可直接执行的AI任务卡。基于您的AI辅助开发优势，这个方案充分考虑了：

1. **技术可行性**：选择成熟稳定的技术栈，避免过度复杂的技术方案
2. **开发效率**：任务拆解精细，每个任务4-12小时，适合AI辅助开发
3. **风险控制**：识别了关键技术风险并提供应对方案
4. **渐进式交付**：按周规划，每周都有可演示的成果
5. **质量保证**：每个任务卡都包含明确的验收标准和测试策略

您可以从Week 1的P0任务开始，逐步构建完整的CMS系统。如果在执行过程中遇到任何技术难题，可以随时通过AI工具获得进一步的帮助。